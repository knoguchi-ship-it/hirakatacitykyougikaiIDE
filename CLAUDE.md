# CLAUDE.md
# 枚方市介護支援専門員連絡協議会 会員システム

> このファイルは後方互換のために保持する。
> 実体ルールは `AGENTS.md` に一本化した。

起動後は必ず `AGENTS.md` を最初に読み、そのルールに従うこと。

`GLOBAL_GROUND_RULES/` にあるのは**詳細ルールと知識ベースだけ**で、常設指示の本体は置いていない。
読む順序の正本は `AGENTS.md` §2。
