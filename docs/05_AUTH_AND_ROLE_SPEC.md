# 認証・権限仕様書

## 1. 目的
- 会員ログインと管理者ログインの方式を明確に分離し、判定ロジックの齟齬を防ぐ。
- 管理者が自分の会員マイページも利用できる要件を運用ルールとして固定する。

## 2. 認証方式
1. 会員ログイン
- 方式: `ログインID + パスワード`
- 使用テーブル: `T_認証アカウント`（`認証方式=PASSWORD`）
- Googleログインは使用しない。

2. 管理者ログイン
- 方式A（**本番標準**）: **セッション認証** — `google.script.run` 経由で `checkAdminBySession_()` を呼び出し、`Session.getActiveUser().getEmail()` で呼び出し元の Google メールを取得し、`T_管理者Googleホワイトリスト` のメール列と突合する。
  - フロントエンドから IDトークンを送信する必要がない。
  - GAS の iframe sandbox 制約（`script.googleusercontent.com` origin）で OAuth ポップアップが開けないため、この方式を採用。
- 方式B（**補完/レガシー互換**）: **IDトークン検証** — `adminGoogleLogin_()` で Google ID トークンを受け取り、`verifyGoogleIdToken_()` で検証後、`GoogleユーザーID` または `Googleメール` でホワイトリスト照合する。
- 使用テーブル（共通）: `T_管理者Googleホワイトリスト`
- 許可条件（共通）:
  - `T_管理者Googleホワイトリスト` に `有効フラグ=true` で Googleメールが存在
  - `紐付け認証ID` → `T_認証アカウント` に紐付きレコードが存在
  - `紐付け会員ID` → `T_会員` に紐付きレコードが存在
- セキュリティ留意点:
  - 方式Aは `google.script.run` の呼び出し元認証に依存するため、GAS Web App を `Execute as: Me / Who has access: Anyone` で公開した状態でも、セッションが取得できるのはブラウザでログイン中の Google ユーザーのみ。
  - 方式Bの IDトークン検証は GAS 組込みの `UrlFetchApp` を使用して Google の公開鍵と突合する。

## 2.1 パスワードセキュリティ仕様
- **ハッシュ方式**: `SHA-256`（GAS 組込み `Utilities.computeDigest(Utilities.DigestAlgorithm.SHA_256, ...)`）+ ランダムソルト（UUID形式）
  - **GAS 制約**: GAS ネイティブは `Utilities.computeDigest` のみ提供。Argon2id / bcrypt / scrypt は **未サポート**。
  - **OWASP 2025 勧告**: パスワードハッシュには `Argon2id`（第1推奨）/ `bcrypt`（第2推奨）を使用すること（[OWASP Password Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)）。
  - **リスク**: SHA-256 + Salt はブルートフォース攻撃に対して bcrypt より脆弱。GAS 外部 API 呼び出し（Apps Script → Node.js 等）によるハッシュ計算への移行を将来検討課題とする。
- **パスワード平文送信**: ログイン・変更時はJSON ペイロードに含めて送信（HTTPS 必須）。GAS 側でハッシュ化後、平文は保持しない。
- **アカウントロック**: `T_認証アカウント.ログイン失敗回数` でカウント、`ロック状態=true` でログイン拒否する機能を実装済み。
  - **⚠️ 既知課題**: ロック解除は DB を直接編集（手動）のみ。自動解除（時間ベース）・管理 UI は未実装。
- **パスワード変更フロー（実装済み）**:
  - **入力**: `{loginId, currentPassword, newPassword}`。
  - **検証**: `changePassword_()` で保存済みソルトを用いた `currentPassword` のハッシュ照合を実施し、不一致時は更新を拒否する。
  - **失敗時制御**: 不一致時は `ログイン失敗回数` を加算し、閾値超過で `ロック状態=true` に遷移する。
  - **成功時制御**: 新PW生成 → ハッシュ保存 → `パスワード更新日時` 更新 → ログイン失敗回数リセット → ロック解除。

## 3. 権限表示ルール

### 3.0 事業所職員の3階層ロール（v99追加）
事業所会員の職員には以下の3階層の権限が設定される。

| ロール | コード | 権限範囲 |
|--------|--------|----------|
| 代表者 | `REPRESENTATIVE` | 全職員の管理（追加・編集・削除）、代表者の変更、事業所情報の編集 |
| 管理者 | `ADMIN` | 職員の追加・編集・削除（代表者ロールの変更は不可） |
| 一般 | `STAFF` | 自分の情報のみ閲覧・編集 |

**制約ルール:**
- 事業所には必ず1名の代表者（REPRESENTATIVE）が存在しなければならない。
- 代表者の変更は代表者自身のみが実行可能（管理者は不可）。
- 入会申込時、代表者は必須（バックエンドでバリデーション）。

1. 会員マイページ種別
- 個人会員
- 事業所会員（代表者）
- 事業所会員（管理者）
- 事業所会員（一般）

2. 左メニュー表示
- 会員ログイン: `会員マイページ` のみ
- 管理者ログイン: `会員マイページ` + `管理者ページ`

3. 管理者の会員情報紐付け
- 管理者アカウントにも必ず `会員ID` を紐付ける。
- 管理者ログイン中も、自分の会員情報を会員マイページで参照できる。

4. 管理コンソール アクセス権限（2026-03-13 追加）
- `T_管理者Googleホワイトリスト` に `有効フラグ=true` で登録されたアカウントは、**会員管理コンソール・年会費管理コンソール・研修管理コンソール** の全てにアクセスできる（同一権限）。
- 両コンソール間は双方向ナビゲーションで遷移可能。再認証は不要（セッション継続中）。
- 会員ログイン（ID/PW）のみのアカウントには、コンソール間ナビゲーションリンクを表示しない。
- 認証判定は両コンソールで共通して `checkAdminBySession_()` を使用する。

## 3.1 フィールドレベルアクセス制御（v105〜）

会員マイページにおける編集可否はサーバーサイド allowlist（`MEMBER_WRITABLE_FIELDS_`）で強制する。
フロントエンドの `disabled` 制御は UX 補助であり、セキュリティ境界はバックエンドが担う。

### 個人会員 — マイページ全項目一覧

#### ヘッダー部（表示のみ）

| フィールド | 表示ラベル | 編集可否 |
|------------|------------|----------|
| `type` | 会員種別 | 表示のみ（「正会員 (個人)」） |
| `id` | 登録番号 | 表示のみ |
| `loginId` | ログインID | 表示のみ |
| `status` | 会員ステータスバッジ | 表示のみ（有効会員/退会予定/退会済み） |
| `annualFeeHistory` | 年会費納入状況（過去2年） | 表示のみ（未納時は振込先口座を表示） |

#### 認証情報

| フィールド | 表示ラベル | 編集可否 |
|------------|------------|----------|
| `loginId` | ログインID | 表示のみ（disabled 固定） |
| — | パスワード変更 | 操作可（現在PW + 新PW + 確認PW） |

#### 基本情報セクション

| フィールド | 表示ラベル | 編集可否 | 備考 |
|------------|------------|----------|------|
| `lastName` | 氏（姓） | 編集可 | 必須 |
| `firstName` | 名（名） | 編集可 | 必須 |
| `lastKana` | フリガナ（セイ） | 編集可 | 必須 |
| `firstKana` | フリガナ（メイ） | 編集可 | 必須 |
| `careManagerNumber` | 介護支援専門員番号 | **disabled** | ログインIDと紐づくため管理コンソール専用。必須 |
| `status` | 会員ステータス | disabled | 管理コンソール専用 |
| `joinedDate` | 入会日 | 表示のみ（編集不可） | |
| `withdrawnDate` | 退会日 | **非表示** | |
| `midtermWithdrawal` | 年度中退会 | 非表示（廃止予定） | 不要 |

#### 自宅情報セクション

| フィールド | 表示ラベル | 編集可否 | 備考 |
|------------|------------|----------|------|
| `homePostCode` | 郵便番号 | 編集可 | 送付先が自宅の場合は必須 |
| `homePrefecture` | 都道府県 | 編集可 | 同上 |
| `homeCity` | 市区町村 | 編集可 | 同上 |
| `homeAddressLine` | 番地・建物名 | 編集可 | 同上 |
| `mobilePhone` | 電話番号 | 編集可 | 必須 |

#### 勤務先（事業所）情報セクション

| フィールド | 表示ラベル | 編集可否 | 備考 |
|------------|------------|----------|------|
| `officeName` | 事業所名 | 編集可 | 勤務なしの場合は「勤務なし」と入力 |
| `officeNumber` | 事業所番号 | 編集可 | |
| `officePostCode` | 郵便番号 | 編集可 | 事業所情報入力時は必須 |
| `officePrefecture` | 都道府県 | 編集可 | 同上 |
| `officeCity` | 市区町村 | 編集可 | 同上 |
| `officeAddressLine` | 番地・建物名 | 編集可 | 同上 |
| `phone` | 電話番号 | 編集可 | |
| `fax` | FAX番号 | 編集可 | 必須 |

#### 発送・通信設定

| フィールド | 表示ラベル | 編集可否 | 備考 |
|------------|------------|----------|------|
| `mailingPreference` | 受取方法 | 編集可 | メール配信（推奨）/ 郵送 |
| `email` | メールアドレス | 編集可 | メール配信選択時は必須 |
| `preferredMailDestination` | 送付先 | 編集可 | 自宅 / 事業所 |

#### 研修申込

| 操作 | 可否 |
|------|------|
| 研修一覧 閲覧 | 可 |
| 研修申込 | 可 |
| 研修取消 | 可 |

#### 退会セクション

| 操作 | 可否 | 備考 |
|------|------|------|
| 退会申請 | 可 | パスワード再認証必須 |
| 退会取消 | 可 | パスワード再認証必須 |

### 管理コンソールのみ編集可能なフィールド（会員マイページでは disabled / 非表示）

| フィールド | 制御方法 | 理由 |
|------------|----------|------|
| `careManagerNumber`（介護支援専門員番号） | disabled + allowlist 除外 | ログインIDと紐づくため会員自身による変更を禁止 |
| `status`（会員ステータス） | disabled | 管理コンソール専用 |
| `joinedDate`（入会日） | 表示のみ（編集不可） | 会員マイページでも表示する |
| `withdrawnDate`（退会日） | 非表示 | 会員マイページには表示しない |
| `midtermWithdrawal`（年度中退会フラグ） | 非表示（廃止予定） | 不要 |

### 個人会員 vs 賛助会員 全項目比較表

凡例: ○=編集可 △=表示のみ ×=非表示 ●=操作可

**ヘッダー部**

| フィールド | 表示ラベル | 個人会員 | 賛助会員 | 差分 |
|------------|------------|----------|----------|------|
| `type` | 会員種別 | △「正会員 (個人)」 | △「賛助会員」 | **表示名** |
| `id` | 登録番号 | △ | △ | |
| `loginId` | ログインID | △（介護支援専門員番号8桁） | △（**9始まり9桁**） | **体系** |
| `status` | 会員ステータスバッジ | △ | △ | |
| `annualFeeHistory` | 年会費納入状況 | △ | △ | |

**認証情報**

| フィールド | 表示ラベル | 個人会員 | 賛助会員 | 差分 |
|------------|------------|----------|----------|------|
| `loginId` | ログインID | △ | △ | |
| — | パスワード変更 | ● | ● | |

**基本情報**

| フィールド | 表示ラベル | 個人会員 | 賛助会員 | 差分 |
|------------|------------|----------|----------|------|
| `lastName` | 氏（姓） | ○ | ○ | |
| `firstName` | 名（名） | ○ | ○ | |
| `lastKana` | フリガナ（セイ） | ○ | ○ | |
| `firstKana` | フリガナ（メイ） | ○ | ○ | |
| `careManagerNumber` | 介護支援専門員番号 | △（必須） | △（**任意**） | **必須/任意** |
| `status` | 会員ステータス | △ | △ | |
| `joinedDate` | 入会日 | △（編集不可） | △（編集不可） | |
| `withdrawnDate` | 退会日 | × | × | |
| `midtermWithdrawal` | 年度中退会 | × | × | |

**自宅情報**

| フィールド | 表示ラベル | 個人会員 | 賛助会員 | 差分 |
|------------|------------|----------|----------|------|
| `homePostCode` | 郵便番号 | ○ | ○ | |
| `homePrefecture` | 都道府県 | ○ | ○ | |
| `homeCity` | 市区町村 | ○ | ○ | |
| `homeAddressLine` | 番地・建物名 | ○ | ○ | |
| `mobilePhone` | 電話番号 | ○ | ○ | |

**勤務先（事業所）情報**

| フィールド | 表示ラベル | 個人会員 | 賛助会員 | 差分 |
|------------|------------|----------|----------|------|
| `officeName` | 事業所名 | ○ | ○ | |
| `officeNumber` | 事業所番号 | ○ | ○ | |
| `officePostCode` | 郵便番号 | ○ | ○ | |
| `officePrefecture` | 都道府県 | ○ | ○ | |
| `officeCity` | 市区町村 | ○ | ○ | |
| `officeAddressLine` | 番地・建物名 | ○ | ○ | |
| `phone` | 電話番号 | ○ | ○ | |
| `fax` | FAX番号 | ○ | ○ | |

**発送・通信設定**

| フィールド | 表示ラベル | 個人会員 | 賛助会員 | 差分 |
|------------|------------|----------|----------|------|
| `mailingPreference` | 受取方法（メール/郵送） | ○ | ○ | |
| `email` | メールアドレス | ○ | ○ | |
| `preferredMailDestination` | 送付先（自宅/事業所） | ○ | ○ | |

**研修・退会**

| 操作 | 個人会員 | 賛助会員 | 差分 |
|------|----------|----------|------|
| 研修一覧 閲覧 | ● | ● | |
| 研修申込 | ● | ● | |
| 研修取消 | ● | ● | |
| 退会申請 | ● | ● | |
| 退会取消 | ● | ● | |

### 事業所会員（代表者 REPRESENTATIVE）— マイページ項目仕様

代表者は `isReadOnly = false`（編集権限あり）。

#### ヘッダー部（表示のみ）

個人会員と共通: 会員種別（「正会員 (事業所)」）、登録番号、ログインID、会員ステータスバッジ、年会費納入状況（過去2年）。

#### 認証情報

個人会員と共通: ログインID（表示のみ）、パスワード変更（編集可）。

#### 基本情報セクション

| フィールド | 代表者の編集可否 | 備考 |
|------------|------------------|------|
| 氏（姓）/ 名（名） | 編集可 | |
| フリガナ（セイ/メイ） | 編集可 | |
| `介護支援専門員番号` | **disabled** | ログインIDと紐づくため管理コンソール以外では編集不可 |
| `会員ステータス` | disabled | 管理コンソール専用 |
| `入会日` | disabled | 管理コンソール専用 |
| `退会日` | disabled | 管理コンソール専用 |
| `年度中退会` | **非表示（廃止予定）** | 退会しても今年度中は会員扱いとするため不要 |

#### 自宅情報セクション

事業所会員には **非表示**。

#### 事業所（勤務先）情報セクション

| フィールド | 代表者の編集可否 | 備考 |
|------------|------------------|------|
| 事業所名 | **表示のみ（編集不可）** | 変更は問い合わせ窓口で受付（方法は別途検討） |
| 事業所番号 | **表示のみ（編集不可）** | 同上 |
| 郵便番号 / 都道府県 / 市区町村 / 番地 | 編集可 | |
| 電話番号 / FAX番号 | 編集可 | |

#### 所属職員一覧

代表者は自分を含む全職員の情報を管理する。

**職員1名あたりのフィールド:**

| フィールド | 表示ラベル | 代表者の編集可否 | 備考 |
|------------|------------|------------------|------|
| `name` | 氏名 | 編集可 | |
| `kana` | フリガナ | 編集可 | |
| `email` | メールアドレス | 編集可 | **メール配信先の設定を兼ねる** |
| `role` | 権限 | 編集可（**代表者のみ**） | 選択肢: 管理者 / 一般（REPRESENTATIVE は選択肢に含めない）。ADMIN ロールでは変更不可 |
| `status` | 状態 | 編集可（在籍→退職） | 代表者が「退職」に変更可能。**退職にした日付をバックエンドで自動記録** |
| `joinedDate` | 登録日 | **表示のみ（編集不可）** | 職員レコード作成時にバックエンドで自動セット |
| `withdrawnDate` | 退職日 | **非表示** | バックエンドで自動記録するがマイページには表示しない |
| `midYearWithdrawal` | — | **廃止** | 不要（今年度中は会員扱い） |
| 削除ボタン | — | **廃止** | 退職ステータスで管理するため物理削除不要 |

**退職者の表示ルール:**
- 退職にしても **今年度中（〜3月31日）は一覧に表示** される。
- **翌年度（4月1日〜）から非表示** になる（データは保持、画面に出さない）。

**職員追加:**
- 代表者は「職員を追加」ボタンで新規職員レコードを作成できる。

#### 発送・通信設定

事業所会員は選択不可。以下の固定ルール表示:
- **郵送物** → 事業所宛に1通送付
- **メール** → 所属職員一覧の各 `email` フィールドに個別配信
- 代表者は自分を含む **全職員のメールアドレスを設定・変更できる**（上記職員一覧の `email` 欄で管理）

#### 研修申込

- 職員一覧から職員名をクリック → **別画面でその職員の研修一覧・申込・取消を表示**。
- **閲覧**: 代表者は全職員分の研修申込状況を閲覧可能。
- **申込・取消操作**: 代表者・管理者（ADMIN）以外の一般職員（STAFF）も自分の分を操作可能。代表者は全職員分を操作可能。

#### 退会セクション

- **代表者のみ** が事業所全体の退会申請・取消を実行可能。
- パスワード再認証必須。
- 退会しても今年度中は会員扱い（サービス利用継続可）。

### 事業所会員（管理者 ADMIN）— マイページ項目仕様

管理者は `isReadOnly = false`（編集権限あり）。代表者との差分を中心に記載。

#### ヘッダー部・認証情報

代表者と共通。

#### 基本情報セクション

代表者と共通: 氏名・フリガナは編集可。介護支援専門員番号・会員ステータス・入会日・退会日は disabled。年度中退会は非表示（廃止予定）。

#### 自宅情報セクション

事業所会員には **非表示**（代表者と共通）。

#### 事業所（勤務先）情報セクション

代表者と共通: 事業所名・事業所番号は表示のみ。住所・電話・FAX は編集可。

#### 所属職員一覧 — 代表者との差分

| フィールド | 代表者 | 管理者（ADMIN） |
|------------|--------|-----------------|
| 氏名 / フリガナ / メールアドレス | 編集可 | 編集可（**同一**） |
| 権限（`role`） | **編集可** | **表示のみ（編集不可）** |
| 状態（在籍→退職） | 編集可 | 編集可（**同一**） |
| 登録日 | 表示のみ | 表示のみ（**同一**） |
| 退職日 | 非表示 | 非表示（**同一**） |
| 職員追加 | 可 | 可（**同一**） |

- 退職者の表示ルール・削除ボタン廃止は代表者と共通。

#### 発送・通信設定

代表者と共通。管理者も全職員のメールアドレスを設定・変更できる。

#### 研修申込

- **閲覧**: 管理者は全職員分の研修申込状況を閲覧可能。
- **申込・取消操作**: 管理者も全職員分を操作可能。

#### 退会セクション

**非表示**。退会申請は代表者のみの権限。

### 事業所会員（一般 STAFF）— マイページ項目仕様

一般職員は `isReadOnly = true`（閲覧専用）。画面上部に「閲覧専用モード」アラートを表示。

#### ヘッダー部（表示のみ）

代表者・管理者と共通: 会員種別、登録番号、ログインID、会員ステータスバッジ、年会費納入状況。

#### 認証情報

| フィールド | 一般職員の操作 | 備考 |
|------------|----------------|------|
| ログインID | 表示のみ | |
| パスワード変更 | **操作可** | 自分のパスワードは一般職員でも変更できる |

#### 基本情報セクション — 全フィールド表示のみ

代表者情報（氏名・フリガナ）、介護支援専門員番号、会員ステータス、入会日、退会日がすべて disabled で表示される。

#### 所属職員一覧

他の職員の情報は表示のみ。**自分の氏名・フリガナ・個別メールアドレスのみ編集可能**。
権限・状態・登録日は表示のみ。退職日は非表示。
職員追加ボタンは非表示。

#### 事業所（勤務先）情報セクション — 全フィールド表示のみ

事業所名・事業所番号・住所・電話・FAX がすべて disabled で表示される。

#### 発送・通信設定

代表者・管理者と共通（固定ルール表示のみ）。

#### 研修申込

- **閲覧**: 自分の研修申込状況を閲覧可能。
- **申込・取消操作**: **自分の分のみ操作可能**。
- 別画面で自分の研修一覧・申込・取消を表示。

#### 退会セクション

**非表示**。退会申請は代表者のみの権限。

#### 保存ボタン

**非表示**（`isReadOnly` 時は描画されない）。

### 事業所会員 3ロール比較表

#### 会員情報・事業所情報

| 項目 | 代表者 | 管理者 | 一般 |
|------|--------|--------|------|
| 氏（姓）/ 名（名） | 編集可 | 編集可 | 表示のみ |
| フリガナ（セイ/メイ） | 編集可 | 編集可 | 表示のみ |
| 介護支援専門員番号 | 表示のみ | 表示のみ | 表示のみ |
| 会員ステータス | 表示のみ | 表示のみ | 表示のみ |
| 入会日 | 表示のみ | 表示のみ | 表示のみ |
| 退会日 | 表示のみ | 表示のみ | 表示のみ |
| 事業所名 | 表示のみ | 表示のみ | 表示のみ |
| 事業所番号 | 表示のみ | 表示のみ | 表示のみ |
| 事業所 郵便番号 | 編集可 | 編集可 | 表示のみ |
| 事業所 都道府県 | 編集可 | 編集可 | 表示のみ |
| 事業所 市区町村 | 編集可 | 編集可 | 表示のみ |
| 事業所 番地・建物名 | 編集可 | 編集可 | 表示のみ |
| 事業所 電話番号 | 編集可 | 編集可 | 表示のみ |
| 事業所 FAX番号 | 編集可 | 編集可 | 表示のみ |

#### 所属職員一覧（職員1名あたり）

| フィールド | 表示ラベル | 代表者 | 管理者 | 一般 |
|------------|------------|--------|--------|------|
| `name` | 氏名 | 編集可 | 編集可 | **自分のみ編集可** |
| `kana` | フリガナ | 編集可 | 編集可 | **自分のみ編集可** |
| `email` | 個別メールアドレス | 編集可 | 編集可 | **自分のみ編集可** |
| `role` | 権限（管理者 / 一般） | **編集可** | **表示のみ** | 表示のみ |
| `status` | 状態（在籍 / 退職） | 編集可 | 編集可 | 表示のみ |
| `joinedDate` | 登録日 | 表示のみ | 表示のみ | 表示のみ |
| `withdrawnDate` | 退職日 | **非表示** | **非表示** | **非表示** |
| — | 職員追加ボタン | 可 | 可 | 不可 |

> 退職日はバックエンドで自動記録するが、マイページ上には表示しない。

#### その他機能

| 項目 | 代表者 | 管理者 | 一般 |
|------|--------|--------|------|
| パスワード変更 | 可 | 可 | 可 |
| 研修 — 閲覧 | 全員分 | 全員分 | **自分のみ** |
| 研修 — 申込・取消 | 全員分 | 全員分 | **自分のみ** |
| 退会申請 | **可** | 不可 | 不可 |
| 保存ボタン | 表示 | 表示 | 非表示 |

### 賛助会員（SUPPORT）— マイページ項目仕様

**※ 未確定。上記「個人会員 vs 賛助会員 全項目比較表」をもとに決定予定。**

## 4. 会費・研修の表示仕様
1. 年会費
- 引き落とし機能は実装しない。
- 未納時のみ振込先口座を表示する。
- 管理者のみ年会費レコードの登録・更新を実行できる。
- 年会費の登録・更新時は監査ログを残す。

2. 研修
- 受付中研修は `概要/詳細` を表示する。
- 案内文PDFを閲覧できるリンクを表示する。

## 5. 受け入れ基準（DoD）
1. 認証
- 会員ID/PWでログイン成功し、Googleログインなしで会員利用可能。
- ホワイトリスト外Googleアカウントは管理者ログイン不可。

2. 権限
- 管理者ログイン時のみ `管理者ページ` が左メニューに出る。
- 管理者ログイン時に `会員マイページ` も利用可能。
- 管理者ログイン時のみ `年会費管理コンソール` が左メニューに出る。

3. 監査
- `T_ログイン履歴` に `認証方式` が記録される。

4. 会費・研修
- 未納時のみ振込先表示が出る。
- 管理者のみ年会費を更新でき、更新履歴が保存される。
- 受付中研修の詳細とPDFが閲覧できる。

## 6. 設定済みサービス・権限一覧（2026-02-28時点）
対象プロジェクト: `uguisu-gas-exec-20260225191000`

### 6.1 GCP API（有効化済み）
1. `script.googleapis.com`（Apps Script API）
2. `drive.googleapis.com`（Google Drive API）
3. `logging.googleapis.com`（Cloud Logging API）

証跡:
- `docs/assets/gcp-setup-2026-02-28/01_cloud_logging_api_enabled.png`
- `docs/assets/gcp-setup-2026-02-28/02_apps_script_api_enabled.png`
- `docs/assets/gcp-setup-2026-02-28/03_drive_api_enabled.png`

### 6.2 Google Auth Platform（OAuth）
1. 対象ユーザー: `内部（Internal）`
2. OAuthクライアント（主要）
- `admin-google-login-web`（種類: ウェブ アプリケーション）
  - JavaScript 生成元: `https://script.google.com`
  - **現状**: 本番標準はセッション認証（方式A）に移行済みのため、このクライアントは方式B（IDトークン検証）用途で保持。削除しない。
3. 既存クライアント
- `Apps Script`（自動生成）
- `デスクトップ クライアント: 1`（clasp ログイン用途）

証跡:
- `docs/assets/gcp-setup-2026-02-28/04_auth_audience_internal.png`
- `docs/assets/gcp-setup-2026-02-28/05_auth_clients_list.png`

### 6.3 Apps Script 側（高度なサービス）
1. 高度なサービス: `Drive API` を追加済み

証跡:
- `docs/assets/gcp-setup-2026-02-28/06_apps_script_drive_service.png`

### 6.4 clasp 認証で要求される主なスコープ
1. `https://www.googleapis.com/auth/script.projects`
2. `https://www.googleapis.com/auth/script.deployments`
3. `https://www.googleapis.com/auth/script.webapp.deploy`
4. `https://www.googleapis.com/auth/drive.file`
5. `https://www.googleapis.com/auth/drive.metadata.readonly`
6. `https://www.googleapis.com/auth/logging.read`
7. `openid`, `email`, `profile`, `userinfo.email`, `userinfo.profile`
8. `https://www.googleapis.com/auth/cloud-platform`
9. `https://www.googleapis.com/auth/gmail.send`（管理コンソール メール送信機能・2026-03-13追加。`appsscript.json` の `oauthScopes` に追記必要）

## 8. 公開ポータルのセキュリティ・個人情報保護法対応（2026-03-12 追加）

### 8.1 認証方針
| 操作 | 認証 | 検証場所 |
|------|------|----------|
| 受付中研修一覧の閲覧 | 不要 | — |
| 非会員として研修申込 | 不要（個人情報入力で代替） | `applyTrainingExternal_()` でサーバー側バリデーション |
| 非会員申込の取消 | 申込ID + 登録メール一致 | `cancelTrainingExternal_()` でサーバー側検証 |
| 研修の登録・編集 | 管理者 Google 認証 | `checkAdminBySession_()` 必須（会員ポータルと同一） |

### 8.2 個人情報保護法対応（必須要件）
根拠：個人情報保護法 第17条（利用目的の特定）・第18条（利用目的の通知）・医療介護関係事業者ガイダンス（2025年6月改正）

| 要件 | 実装方針 | 根拠条文 |
|------|----------|----------|
| 利用目的の明示 | フォームに「収集した個人情報は研修申込の受付・確認連絡のみに使用します」を明記。抽象的表現禁止 | 法第17条 |
| 同意取得 | 送信ボタン前に「プライバシーポリシーに同意する」チェックボックスを必須配置。未チェックは送信不可 | 法第17条・18条 |
| データ保管期間 | 研修終了日の翌年4月1日以降に `削除フラグ=true` を自動適用（`applyWithdrawalDeletionPolicy_()` 拡張） | 法第19条（利用する必要がなくなったときは速やかに消去する努力義務） |
| 第三者提供の禁止 | 収集データは本システム内のみ。外部サービス・第三者への提供なし | 法第27条 |
| 同意日時の記録 | `T_外部申込者.同意日時` に記録し、同意証跡を保持 | ガイダンス準拠 |

### 8.3 入力バリデーション（OWASP準拠）
根拠：[OWASP Input Validation Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Input_Validation_Cheat_Sheet.html)

- **サーバー側バリデーション必須**（クライアント側のみでは不十分）
- メールアドレス: 正規表現 + 最大長チェック
- 電話番号: 数字・ハイフンのみ許容
- フリガナ（セイ/メイ）: カタカナのみ許容
- 郵便番号: `123-4567` 形式を必須とする
- 介護支援専門員番号: 8桁の数字を必須とする
- 事業所番号: 数字のみ許容

### 8.3.1 新規入会申込フォームの初期値

- 個人会員の住所情報は初期表示で `大阪府` / `枚方市` / `573-` を設定する
- 個人会員で勤務先または自宅の片側しか入力しない場合、未使用側のデフォルト都道府県・郵便番号・市区町村は DB に保存しない
- 事業所会員の事業所情報は初期表示で `大阪府` / `枚方市` / `573-` を設定する
- 事業所会員の職員登録フォームは初期表示で3名分を表示する
- 初期表示の職員3名は `代表者 1名 + 職員 2名` とする
- 氏名: 最大長チェック（スクリプトインジェクション対策）
- 申込期間: `申込開始日`〜`申込締切日` をサーバー側で必ず検証

### 8.4 スパム・ボット対策
GAS の制約（外部 JS サービス利用不可）のため以下の実装可能な手段で対応：
- **Honeypot フィールド**: フォームに `display:none` の隠し入力フィールドを設置。入力値が空でない場合はサーバー側で申込を無条件拒否
- **重複申込チェック**: 同一メールアドレス + 同一研修 ID の重複申込を `applyTrainingExternal_()` でサーバー側検出・拒否
- **申込期間チェック**: 期間外申込はサーバー側で拒否

## 7. 現在設定状況の確認方法
1. GCP API確認
- Google Cloud Console > `API とサービス` > `有効な API とサービス`
- `Apps Script API / Google Drive API / Cloud Logging API` が Enabled であること

2. OAuth設定確認
- Google Cloud Console > `Google Auth Platform` > `対象`
- `内部` になっていること
- Google Cloud Console > `Google Auth Platform` > `クライアント`
- `admin-google-login-web` が存在すること

3. Apps Script 高度なサービス確認
- Apps Script エディタ左ペイン `サービス`
- `Drive` が表示されること

4. ローカルCLI確認（補助）
- `gcloud config get-value project` が `uguisu-gas-exec-20260225191000` を返す
- `gcloud auth list` の ACTIVE が `k.noguchi@uguisunosato.or.jp` である
- `npx clasp apis` で `drive` が有効表示される
