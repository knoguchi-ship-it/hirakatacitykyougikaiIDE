# 引き継ぎ書（2026-02-25）

## 1. プロジェクト概要
- 対象: 枚方市介護支援専門員連絡協議会 会員システム（GAS + スプレッドシートDB）
- ローカル作業ディレクトリ: `C:\VSCode\CloudePL\hirakatacitykyougikaiIDE`
- Apps Script ID: `11YRlyWVgWRFw5_zByfLnA_vUlZzLeBSgiaanQCvZZoHMAfay8yK7RdkL`

## 2. 絶対運用ルール（重要）
- 使用Googleアカウントは `k.noguchi@uguisunosato.or.jp` のみ。
- 他アカウントは、ユーザーから明示指示がある場合のみ使用。

## 3. 実施済み内容
- `clasp` の紐付け整理・再作成。
- GASコードを本番向けに push/deploy（複数回更新）。
- スプレッドシートDBを作成:
  - 名称: `枚方市介護支援専門員連絡協議会_DB`
  - ID: `1GVlIzOG1Tsqw8fBXgZ__c8u4oMu-4_WCf0H3aVLESKs`
  - URL: `https://docs.google.com/spreadsheets/d/1GVlIzOG1Tsqw8fBXgZ__c8u4oMu-4_WCf0H3aVLESKs/edit`
- `setupDatabase()` 実行で、マスタ/テーブルを自動作成済み。
- ドキュメント整備:
  - `docs/03_DATA_MODEL.md`（設計 + 構築記録）
  - `docs/04_DB_OPERATION_RUNBOOK.md`（運用手順）

## 4. 実装済み関数（`backend/Code.gs`）
- `setupDatabase()`: DB生成/初期化（定義外シート削除含む）
- `rebuildDatabaseSchema()`: 再構築（定義外シート削除含む）
- `cleanupDatabaseSheets()`: 定義外シート削除専用
- `setDatabaseSpreadsheetId(spreadsheetId)`: 参照先DB設定
- `DB_SPREADSHEET_ID_FIXED`: 上記DB IDに固定設定済み

## 5. 現在の状況
- DBシート群（マスタ/テーブル）は作成済み。
- `シート1` が残っている可能性があり、削除は `cleanupDatabaseSheets()` 実行で対応可能。
- `clasp run` は未解消:
  - エラー: `Script function not found. Please make sure script is deployed as API executable.`
  - 原因: Apps Script の API executable 設定/デプロイ未整備。

## 6. 次担当者の最優先タスク
1. Apps Scriptエディタで `cleanupDatabaseSheets()` を実行し、不要シートを削除。
2. Apps Script のデプロイ設定で `API executable` を有効化。
3. `clasp run --nondev getDbInfo` が通ることを確認。
4. 本番WebアプリURLが 404 でないことを確認。

## 7. 参照コマンド
- GASを開く: `npx clasp open-script`
- 反映: `npm run build:gas && npx clasp push --force`
- デプロイ一覧: `npx clasp deployments`
- 実行確認: `npx clasp run --nondev getDbInfo`

## 8. 関連ファイル
- `backend/Code.gs`
- `backend/appsscript.json`
- `docs/03_DATA_MODEL.md`
- `docs/04_DB_OPERATION_RUNBOOK.md`
