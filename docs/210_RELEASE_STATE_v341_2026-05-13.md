# Release State v341

Date: 2026-05-13
Status: production deployed

## Scope

- Fixed the admin annual fee management console -> member detail navigation.
- Kept `T_会員.会員ID` as the canonical key across `AnnualFeeAdminRecord.memberId` and `Member.id`.
- Changed the navigation path to use `openMemberDetail(memberId)` instead of directly switching to `member-detail`.
- Added a member-detail snapshot in React state so the detail page is not dependent on the admin member list being loaded.
- No DB schema change.
- No public portal functional change.

## Deployment

| Target | Version | Fixed deployment |
|---|---:|---|
| integrated/public legacy | `301` unchanged | `AKfycbywpWoYxij6A-ZunIeBjG1Q8qX78PMMTsT3frx1cM5PJ2nAuZpz81KruXb5LIvWgbQx` |
| integrated/public | `301` unchanged | `AKfycbxyuUXgK1oHUDMahQjluiL-gcrMK0qV0FWLFYaYBqGxlRSg9NhvmbyQRyf0dvaqg7Zp` |
| member split | `58` | `AKfycbxd_6HlH5aWLhxYOtLUHehI3ODiHg4fpc5SCzNdEBIDbDpaBuU3KTuqDRbeBmhWZxSQ_g` |
| admin split | `99` | `AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os` |

Deployment description: `v341 member detail routing fix`

## Verification

- `npm run prerelease`: PASS.
- `npm run build:gas`: PASS.
- `npm run build:gas:member`: PASS.
- `npm run build:gas:admin`: PASS.
- `npm run security:public-boundary`: PASS.
- `npm run security:split-boundary`: PASS.
- `npx clasp push --force` member split: PASS.
- `npx clasp version "v341 member detail routing from annual fee"` member split: created version `58`.
- `npx clasp redeploy ... --versionNumber 58 --description "v341 member detail routing fix"` member split: PASS.
- `npx clasp deployments --json` member split: fixed deployment points to `@58`.
- `npx clasp push --force` admin split: PASS.
- `npx clasp version "v341 member detail routing from annual fee"` admin split: created version `99`.
- `npx clasp redeploy ... --versionNumber 99 --description "v341 member detail routing fix"` admin split: PASS.
- `npx clasp deployments --json` admin split: fixed deployment points to `@99`.
- `npx clasp deployments --json` integrated/public: PASS, both fixed deployments remain `@301`.
- Code inspection: annual fee records and member detail both use `T_会員.会員ID`; no alternate key was introduced.
- Code inspection: `selectedMemberForDetail` falls back to the fetched detail snapshot when the global `members` list has not been loaded.

## Operator Checks

Browser-side checks still required:

1. Admin portal -> 年会費管理.
2. Click a member name before opening/loading 会員管理 -> 会員一覧.
3. Confirm 会員詳細 opens and does not show `会員データが見つかりません。`.
4. Save an unrelated member detail field and confirm the page remains stable after background refresh.
