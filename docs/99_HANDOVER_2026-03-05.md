# 引き継ぎ書（2026-03-05）

## 1. 対象
- プロジェクト: 枚方市介護支援専門員連絡協議会 会員システム
- 作業ディレクトリ: `C:\VSCode\CloudePL\hirakatacitykyougikaiIDE`
- Apps Script ID: `11YRlyWVgWRFw5_zByfLnA_vUlZzLeBSgiaanQCvZZoHMAfay8yK7RdkL`

## 2. 本日時点の確定事項
- 会員機能は Google アカウント不要（ID/パスワード）
- 管理者機能は Google ID トークン検証 + `T_管理者Googleホワイトリスト` 照合
- Web アプリ `/exec` は有効（新規デプロイで 200 応答確認）
- メール送信機能を GAS 側に実装済み（`MailApp.sendEmail`）

## 3. メール機能の実装状況
- 追加アクション: `processApiRequest('sendTrainingReminder', payload)`
- 実装関数:
  - `sendTrainingReminder_(request)`
  - `collectTrainingRecipients_(members, trainingId)`
  - `buildTrainingReminderBody_(training)`
- テスト実行用関数:
  - `dryRunTrainingReminderT001()`
  - `sendTrainingReminderToNoguchiTest()`
- 重要運用方針:
  - ドライランは API/CLI でのみ実行する
  - UI にドライランボタンは追加しない

## 4. 実施済み検証結果
1. ドライラン
- コマンド: `npx clasp run dryRunTrainingReminderT001`
- 結果: 成功
- 確認項目:
  - 宛先候補を返却
  - 件名を返却
  - 本文を返却
  - `sentCount: 0`

2. 実送信テスト
- コマンド: `npx clasp run sendTrainingReminderToNoguchiTest`
- 送信先: `k.noguchi@uguisunosato.or.jp`
- 結果: 成功（`sentCount: 1`）

## 5. 本日解消した不具合
- 事象: `MailApp.sendEmail` 実行時の権限不足
- 原因: OAuth scope 未設定
- 対応: `backend/appsscript.json` に `https://www.googleapis.com/auth/script.send_mail` を追加し、`clasp login --use-project-scopes` で再認可

## 6. 認証・運用上の注意
- `clasp push` 時に別アカウントへ切り替わると権限エラーになる
- 運用時は必ず `k.noguchi@uguisunosato.or.jp` で実行する
- 事前確認コマンド: `npx clasp show-authorized-user`

## 7. ドキュメント整合の反映
- `README.md`
  - Backend 表記を「Planned」から現行実装に修正
- `docs/02_ARCHITECTURE.md`
  - メール配信技術表記を `GmailApp` から `MailApp.sendEmail` に修正
  - ドライランは API/CLI のみで運用する方針を反映

## 8. 参照（優先順）
1. `docs/99_HANDOVER_2026-03-05.md`（本書）
2. `docs/05_AUTH_AND_ROLE_SPEC.md`
3. `docs/02_ARCHITECTURE.md`
4. `docs/08_GCP_SETUP_RUNBOOK_2026-02-28.md`
