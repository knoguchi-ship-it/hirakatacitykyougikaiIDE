# v360〜v370 リリース統合正本

更新日: 2026-05-17
現行本番: **v370**（admin HEAD は v370.1 cleanup 関数を含む）
fixed deployment: integrated/public `@329` x2 / member split `@87` / admin split `@129`

> v360 から v370 までを時系列にまとめた包括正本。各リリース個別の詳細は HANDOVER.md §4 と当該 release state 文書を参照。データモデル設計は `docs/03_DATA_MODEL.md` §11 / §12。
>
> **2026-05-18 注記**: security/docs cleanup により、`.clasp.json` は Git 追跡対象外、dryRun 物理削除 callable は admin artifact から削除、研修名簿 UI は CSV 出力のみ、CopyButton は 44px タップターゲットへ修正。現行再開状態は `HANDOVER.md` を正とする。

## 1. 累積変更サマリ

### 1.1 新機能（v360 ファミリ）

| 機能 | リリース | 配置 |
|---|---|---|
| 研修名簿コンソール（閲覧・出欠記録・xlsx 出力→ CSV へ後退）| v360 | admin |
| 研修名簿への会員/職員/ゲスト手動追加 | v360 | admin |
| 研修出欠状態管理（5 段階）| v360 | admin |
| 研修ごとの集計指標（残席・区分内訳・出欠率）| v360 | admin |
| 会員ごとの受講履歴（API のみ・UI 未実装）| v360 | admin |
| セグメントメール送信 + Header-Detail 送達ログ | v360 | admin |
| 会員詳細モーダル化（年会費管理から会員名クリック）| v363.2 | admin |
| 年会費一覧: 前年度未納行ハイライト + サマリカード | v364 | admin |
| 年会費一覧: 会員名コピーボタン（CopyButton 汎用化）| v365 | admin |
| 年会費管理: 申し送りメモを sticky 化 | v366 | admin |
| 申込/承認/却下メールテンプレ化 + システム設定 UI 編集 | v368, v369 | admin/public/member |

### 1.2 検索強化（v362）

`matchesSearchQuery` を強化。半角カナ/全角カナ/全角ひらがな いずれの入力でもフリガナ列にヒット。

正規化順: `trim → toLowerCase → NFKC → ひらがな→カタカナ → NFC`

検索値配列に `kana` を含む箇所: 会員管理 / 年会費管理 / 一括メール / 宛名リスト / 事業所職員ディレクトリ（既存）。

### 1.3 緊急 bug fix

| バグ | 修正 | リリース |
|---|---|---|
| SheetJS xlsx の import.meta が vite-plugin-singlefile + compress-html の `new Function()` で SyntaxError → admin/member shell クラッシュ | xlsx 除去 + CSV のみ + build 時 import.meta 検出 gate | v361 |
| 会員詳細を新タブで開くと GAS DOMAIN 認証で再ログイン要求 → UX 不可 | モーダル方式へ変更 | v363→v363.2 |
| `adminSession.email` 参照（実際は `loginId` キー）で変更申請承認/却下が常に unauthorized | 4 箇所 `loginId` に修正 + dispatcher で inner.success:false 伝播 + ChangeRequestConsole UX 修正 | v367 |
| v368 Logger.log の `srcMemberId` undefined → 事業所入会申込承認で convertIndividualToStaff_ クラッシュ → 代表者 1 名のみ partial 登録 | `sourceMemberId` に修正 + cleanup 関数 (v370.1) 提供 | v370 |

## 2. 配信状態（fixed deployment）

| プロジェクト | Deployment ID | 現行 version | role |
|---|---|---|---|
| 統合 (legacy) | `AKfycbywpWoY...` | **@329** | 公開ポータル（legacy URL）|
| 統合 (official) | `AKfycbxyuUXg...` | **@329** | 公開ポータル（official URL）|
| 会員 split | `AKfycbxd...` | **@87** | 会員マイページ |
| 管理者 split | `AKfycbwS...` | **@129** | 管理者ポータル（admin HEAD は v370.1 関数も保持）|

## 3. operator 残作業（最優先）

### 3.1 v360 schema データ migration（必須・未実行）

admin split の Apps Script editor で **`runRebuildSchemaForV360`** を 1 回 Run。

実行内容:
1. M_出欠状態 マスタ作成 + 5 件初期値投入
2. T_研修申込 に末尾 5 列追加（外部申込者ID / 出欠状態コード / 出欠記録日時 / 出欠記録者メール / 事務局メモ）
3. T_メール送信明細 をログ SS に作成
4. T_メール送信ログ.研修ID 列追加
5. 既存 EXTERNAL 申込行を 2-FK 化（申込者ID → 外部申込者ID 複写）
6. 既存申込行の出欠状態を `UNRECORDED` で backfill
7. ROSTER_TEMPLATE_LIST JSON 各エントリに `category='MAILING_LIST'` auto-add
8. XOR 整合性監査（`xorViolations: 0` を Logger.log で確認）

これを実行しないと一括メール送信機能のみ動作不可（その他は OK）。

### 3.2 v370 partial データ cleanup（実行中）

申請 `CR1778920612878_22c197b0`（枚方市包括支援センターはなまる）で会員ID `53779700` が partial 登録。

admin split の Apps Script editor で **`runCleanupPartialBusinessV370_53779700`** を 1 回 Run → cleanup 結果ログを確認 → 変更申請管理コンソールから再承認 → 3 名全員が正常作成される。

### 3.3 v360〜v370 実ブラウザ動作確認

admin shell 起動確認（v351 罠回避）。`https://script.google.com/a/macros/hcm-n.org/s/AKfycbwS.../exec` を DOMAIN ユーザーで開き、白ページにならず管理者ポータルが表示されることを確認。

### 3.4 動作確認後にコミット

v360-v370 本体と 2026-05-18 cleanup の working tree 差分は未コミット。動作確認後、明示指示で commit 実行可能。`.clasp.json` は Git 追跡対象外として扱う。

## 4. v362〜v370 個別変更詳細

### v370.1 (2026-05-17・admin HEAD push)
- partial application 診断 + cleanup 関数を admin split に追加
- `diagnoseStaleApplicationForV370(requestId?)` 読み取り専用診断
- `diagnoseAllStaleApplicationsForV370()` 全 PENDING 一括診断
- `cleanupStaleBusinessApplicationForV370(memberId)` 部分登録のソフト削除
- `runCleanupPartialBusinessV370_53779700()` 53779700 専用 wrapper（次リリースで削除予定）
- 監査ログ自動記録
- `scripts/audit-admin-boundary.mjs` / `scripts/build-admin-gas.mjs` の allowlist 更新

### v370 (2026-05-17・緊急 hotfix・integrated@329 x2 / member@87 / admin@129)
- `convertIndividualToStaff_` 内 Logger.log の `srcMemberId` → `sourceMemberId`
- 事業所入会申込承認で職員 CM 番号が既存個人会員と一致した時の ReferenceError 解消

### v369 (2026-05-17・integrated@328 x2 / member@86 / admin@128)
- v368 の 9 システム設定キーをシステム設定 UI から編集可能に
- `src/types.ts` SystemSettings 拡張、`getSystemSettings_` / `updateSystemSettings_` 対応
- 「入会・登録メール設定」サブビュー末尾に「変更申請ワークフロー」セクション追加
- 差込変数ガイド表示

### v368 (2026-05-17・integrated@327 x2 / member@85 / admin@127)
- (1) 個人/賛助→事業所職員転籍時の代表メアド必須を緩和（CM 番号紐づけのみで通す）
- (2) 申込/承認/却下メールをハードコードからシステム設定テンプレートへ移行
- `T_システム設定` に 9 新キー追加（APPLICATION_RECEIPT_* / APPROVAL_NOTIFICATION_* / REJECTION_NOTIFICATION_* 各 ENABLED/SUBJECT/BODY）
- 差込変数 9 種類対応、`buildChangeSummaryText_` で変更内容サマリー生成
- `sendApplicationReceiptMail_` / `sendApprovalNotificationMail_` / `sendRejectionNotificationMail_` ヘルパー新設

### v367 (2026-05-17・緊急 bug fix・integrated@326 x2 / member@84 / admin@126)
- `adminSession.email` (undefined) → `adminSession.loginId` 修正（4 箇所）
- dispatcher で inner success:false を outer に伝播（approve/reject）
- ChangeRequestConsole UX: 成功扱いで失敗内容を表示する矛盾を修正

### v366 (2026-05-17・integrated@325 x2 / member@83 / admin@125)
- 年会費管理コンソールの SharedMemoPanel を sm+ で sticky 化
- IntersectionObserver で sentinel が viewport 上方へ消えたタイミング (stuck) を検知 → 自動 collapsed
- モバイル（< 640px）は通常スクロール（Nielsen 推奨: viewport 15% ルール準拠）

### v365 (2026-05-17・integrated@324 x2 / member@82 / admin@124)
- 年会費一覧の会員名横にコピーボタン（汎用 `src/components/CopyButton.tsx`）追加
- `navigator.clipboard.writeText` 優先 + `document.execCommand('copy')` fallback
- 1.5 秒アイコン切替（clipboard → check）+ `aria-live="polite"` + `stopPropagation`

### v364 (2026-05-17・integrated@323 x2 / member@81 / admin@123)
- 年会費一覧で前年度未納行を red-50 背景 + 左ボーダー + ⚠ アイコン + テキストでハイライト
- 前年度在籍判定で「対象外」を除外、UNPAID と未記録のみ警告対象
- 上部サマリに「前年度未納」カード追加
- WCAG 2.2「色だけに頼らない」準拠
- `AnnualFeeAdminRecord` に `previousYear` / `previousYearEligible` / `previousYearStatus` 派生フィールド追加

### v363.2 (2026-05-16・integrated@322 x2 / member@80 / admin@122)
- 会員詳細をモーダルダイアログ表示に変更（v363 新タブ廃止）
- GAS DOMAIN 認証で毎回ログイン画面に戻る問題を回避
- 背景に元コンソール維持 + ✕/ESC/backdrop で閉じる
- `role="dialog"` + `aria-modal` + `aria-label` で a11y 対応

### v363 (2026-05-16・直後 v363.2 で改修)
- 会員詳細を新タブで開く試行
- doGet で `window.__APP_URL__` 注入、hash routing helper (`src/utils/deepLink.ts`)
- GAS DOMAIN 認証で再ログイン要求のため UX 不可と判明
- 注入と deepLink.ts はコード上残置（将来 public ポータルの deep link 用）

### v362 (2026-05-16・integrated@320 x2 / member@77 / admin@118 → v362.2 で admin@120/member@78)
- 管理コンソール全検索のフリガナ対応
- `src/utils/search.ts` に ひらがな→カタカナ統一 + NFC 追加
- `AdminDashboardMemberRow` / `AnnualFeeAdminRecord` / `BulkMailRecipient` / `MailingListTarget` に `kana` 列追加
- バックエンドが返す行に kana 含むよう改修
- `getAnnualFeeAdminCacheKey_` / `getAdminDashboardCacheKey_` を `v362-kana` で bump
- `scripts/test-search.mts` で 16 ケース単体テスト（Node 22 `--experimental-strip-types`、依存追加なし）

### v361 (2026-05-16・hotfix・integrated@319 x2 / member@76 / admin@117)
- v360 で導入した SheetJS xlsx の dynamic import が `import.meta.url` を bundle に残し、`compress-html.mjs` の `new Function()` ラッパーと非互換で admin/member shell をクラッシュさせた問題を修正
- xlsx を完全除去し CSV (UTF-8 BOM) のみ提供
- `scripts/compress-html.mjs` に build 時 `import.meta` 残存検知 gate を追加

### v360 (2026-05-16・integrated@318 x2 / member@75 / admin@116)
- 研修名簿コンソール新設（admin only）
- 新規マスタ M_出欠状態（5 件）
- T_研修申込 末尾 5 列追加: 外部申込者ID / 出欠状態コード / 出欠記録日時 / 出欠記録者メール / 事務局メモ
- 新規テーブル T_メール送信明細（ログ SS）
- T_メール送信ログ 拡張: 研修ID 列追加・送信種別に `TRAINING_BULK` 追加
- ROSTER_TEMPLATE_LIST JSON エントリに `category` フィールド追加
- T_研修申込 を 2-FK XOR 化（外部申込者ID 列を canonical FK として導入、申込者区分コード/申込者ID は deprecated）
- 11 admin-only API action 追加（getTrainingRosterDetail, saveAttendance, addRosterEntry, etc）
- 詳細: `docs/223_RELEASE_STATE_v360_2026-05-16.md`

## 5. 関連 memory（教訓）

- `feedback_pdfjs_dist_vite_singlefile_trap.md` — import.meta × vite-plugin-singlefile の SyntaxError 罠（v351 / v360 で 2 度発生）
- `feedback_build_gas_only_backend.md` — `npm run build:gas` は backend/Code.gs しか更新しない（v362 hotfix の原因）
- `feedback_gas_new_tab_auth_trap.md` — GAS DOMAIN Web App の新タブ deep link は再ログイン地獄（v363 → v363.2）
- `feedback_admin_session_loginId.md` — adminSession のキーは `loginId`（`email` ではない）+ dispatcher 包み罠（v367）

## 6. 既知の制約 / 次回拡張候補

- **研修名簿 MemberSelectModal 未実装**: 名簿への会員追加は現状 ID 直接入力のみ
- **TrainingMailSender セグメント UI 未実装**: GAS API は完成、UI のみ次回
- **会員詳細「受講履歴」タブ未実装**: GAS API は完成、UI のみ次回
- **`申込者区分コード` / `申込者ID` 物理削除**: v370+ で deprecated マーキングのみ。v380 以降で物理削除予定
- **xlsx 出力廃止**: CSV (UTF-8 BOM) 出力を正本とする（Excel で直接開ける）
- **runCleanupPartialBusinessV370_53779700**: 53779700 のクリーンアップ完了後、次リリースで削除予定
