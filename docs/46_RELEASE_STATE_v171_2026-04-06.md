# Release State v171

作成日: 2026-04-06
対象: 本番 `v171` / fixed deployments `@171`
ステータス: 保存版

## 1. この文書の目的
- 2026-04-06 時点の本番コード状態と確認結果を固定記録する。
- 次担当者が「コードは v171、fixed deployment は両系とも @171」という前提で再開できるようにする。

## 2. 本番コード状態
- 事業所会員の代表者情報は `REPRESENTATIVE` 本人のみ編集可能。
- 事業所会員の `ADMIN` は代表者情報を閲覧のみとし、代表者行の状態・権限は会員マイページから変更不可。
- 事業所会員の会員ステータスと入会日は会員マイページで表示のみ。
- `WITHDRAWAL_SCHEDULED` の場合は、退会予定であることと退会予定日を表示する。
- fixed deployment 2 本はともに `@171`。

## 3. 本番 DB 状態
- 本番 DB 基準は 2026-04-04 のロールバック後整合済み状態を継続利用する。
- 今回の `v171` リリースでは DB スキーマ変更・DB 更新は実施していない。

## 4. 当日確認済み事項
- `npx clasp show-authorized-user`: `k.noguchi@uguisunosato.or.jp`
- `npm run typecheck`: 成功
- `npm run build`: 成功
- `npm run build:gas`: 成功
- `npx clasp push --force`: 成功
- `npx clasp version "v171 restrict business representative editing and display withdrawal schedule in member portal"`: 成功
- `npx clasp deployments --json`: member / public ともに `@171`
- `npx clasp run healthCheck`: 成功
- `npx clasp run getDbInfo`: 成功

## 5. 補足
- `npx clasp deployments --json` 実行時に、出力自体は正常だったが credential store 保存で `SyntaxError: Unexpected end of JSON input` が 1 回混在した。少なくとも当時点では deployment 情報取得自体と runtime 疎通は成功している。
- 現在の正本は `HANDOVER.md`、`docs/09_DEPLOYMENT_POLICY.md`、`docs/05_AUTH_AND_ROLE_SPEC.md`、`docs/04_DB_OPERATION_RUNBOOK.md` を優先参照する。
