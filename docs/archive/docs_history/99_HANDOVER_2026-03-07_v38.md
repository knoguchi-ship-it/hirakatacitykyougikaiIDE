# 引き継ぎメモ 2026-03-07 v38

## 1. 反映先
- プロジェクト: 枚方市介護支援専門員連絡協議会 会員システム
- 本番 Deployment ID: `AKfycbwhBtZrskFtLS1BFtXnKEWdm9eGqZkB_115kQUL2iEzR8yAfbU24tmzkmDZ-8WHhyrN`
- 本番 Version: `38`
- 本番 URL: `https://script.google.com/macros/s/AKfycbwhBtZrskFtLS1BFtXnKEWdm9eGqZkB_115kQUL2iEzR8yAfbU24tmzkmDZ-8WHhyrN/exec`

## 2. 今回の対応
- 既存テスト研修データで未設定だった問い合わせ窓口（担当者/連絡先）を補完。
- GAS に監査関数を追加:
  - `auditTrainingInquiryContacts()`
- GAS に一括補完関数を追加:
  - `backfillTrainingInquiryContacts()`
- 既存の必須バリデーション（フロント + API + GAS）との整合を確認。

## 3. 実行ログ（要約）
1. `npm run build` 成功
2. `npm run build:gas` 成功
3. `npx clasp push --force` 成功
4. `npx clasp run auditTrainingInquiryContacts` 実行
  - 結果: `missingCount: 3`（`T001`, `T002`, `T9286F974`）
5. `npx clasp run backfillTrainingInquiryContacts` 実行
  - 結果: `updated: 3`
  - 補完値: `担当者=事務局`, `連絡先=support@example.com`, `種別=EMAIL`
6. `npx clasp run auditTrainingInquiryContacts` 再実行
  - 結果: `missingCount: 0`
7. `npx clasp run getApiDataSnapshot` で補完反映を確認
8. `npx clasp version "v38 ..."` 作成
9. `npx clasp redeploy ... -V 38` 実行

## 4. 現在の整合性
- 問い合わせ窓口の必須項目欠損は実データ上で解消済み。
- 研修登録/変更時の必須チェックは以下3層で統一済み:
  - 画面（`TrainingManagement.tsx`）
  - Mock API（`src/services/api.ts`）
  - GAS（`backend/Code.gs`）

## 5. 運用メモ
- 新規追加・編集時は、問い合わせ窓口の入力が必須。
- 既存データに同様の欠損が発生した場合は以下順で復旧:
  1. `auditTrainingInquiryContacts`
  2. `backfillTrainingInquiryContacts`
  3. `auditTrainingInquiryContacts`（再確認）
