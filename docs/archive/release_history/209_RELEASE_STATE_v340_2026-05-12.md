# Release State v340

Date: 2026-05-12
Status: production deployed

## Scope

- Added the admin-only member status note field.
- Added `T_会員.ステータスメモ` as the last column of `T_会員`.
- Added the memo textarea to the admin member detail status section.
- Kept the value out of member portal and public portal API responses.
- Updated schema initialization so existing table headers are not overwritten before name-based migration.
- Bumped `DB_SCHEMA_VERSION` to `2026-05-12-member-status-note-v1`.

## Deployment

| Target | Version | Fixed deployment |
|---|---:|---|
| integrated/public legacy | `301` | `AKfycbywpWoYxij6A-ZunIeBjG1Q8qX78PMMTsT3frx1cM5PJ2nAuZpz81KruXb5LIvWgbQx` |
| integrated/public | `301` | `AKfycbxyuUXgK1oHUDMahQjluiL-gcrMK0qV0FWLFYaYBqGxlRSg9NhvmbyQRyf0dvaqg7Zp` |
| member split | `57` | `AKfycbxd_6HlH5aWLhxYOtLUHehI3ODiHg4fpc5SCzNdEBIDbDpaBuU3KTuqDRbeBmhWZxSQ_g` |
| admin split | `98` | `AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os` |

Deployment description: `v340 status note schema fix`

## Verification

- `npx clasp deployments --json` integrated/public: PASS, both fixed deployments point to `@301`.
- `npx clasp deployments --json` member split: PASS, fixed deployment points to `@57`.
- `npx clasp deployments --json` admin split: PASS, fixed deployment points to `@98`.
- `npm run prerelease`: PASS.
- `npm run security:public-boundary`: PASS.
- `npm run security:split-boundary`: PASS.
- `npm run security:admin-boundary`: PASS.
- Code inspection: `initializeSchema_()` calls `ensureTableSheetsExist_()` before `normalizeTableColumns_()`, so existing sheets are not header-reset before column-name migration.
- Code inspection: `mapMembersForApi_()` includes `statusNote` only when called with `{ includeAdminStatusNote: true }`; admin dashboard passes the option, member portal does not.
- Code inspection: `MEMBER_WRITABLE_FIELDS_` does not include `statusNote`; admin writable/audit fields include it.

## Residual Checks

- Apps Script Execution API direct function runs from this local session returned `Unable to run script function. Please make sure you have permission to run the script function.` Therefore DB header/row-width inspection by `clasp run` was not completed from the agent session.
- 2026-05-13 handover start: `npx clasp deployments --json` returned `invalid_grant` / `invalid_rapt` for integrated/public, member split, and admin split. Re-authenticate local clasp OAuth before the next Apps Script API verification or deployment operation.
- Operator browser checks still required:
  1. Admin member detail shows `ステータスメモ（管理者のみ）`.
  2. Saving the memo persists after reload.
  3. Member portal does not show the memo.
  4. Existing status/date updates still save correctly.
