# Release State v294

作成日: 2026-05-03
対象本番: `v294`

## 1. Scope

- 宛名リスト出力コンソールの表示文言を「年度処理」から「年会費納入」へ変更。
  - フィルターラベル
  - アクティブフィルターチップ
  - キーワード検索 placeholder
  - 一覧テーブル見出し
- 発送対象読み込み直後の選択状態を、全件選択から未選択へ変更。
- 「表示中を選択」ボタンの強調色を解除し、他の選択操作ボタンと同じ白背景・slate border に統一。
- GAS action / backend logic の追加変更なし。admin HTML artifact のみ更新。

## 2. Versions

| Area | Version |
|---|---|
| Integrated/public GAS version | `290`（変更なし）|
| Member split GAS version | `40`（変更なし）|
| Admin split GAS version | `51` |

## 3. Fixed Deployments

| Area | Deployment ID | Version |
|---|---|---|
| Integrated legacy deployment | `AKfycbywpWoYxij6A-ZunIeBjG1Q8qX78PMMTsT3frx1cM5PJ2nAuZpz81KruXb5LIvWgbQx` | `@290`（変更なし）|
| Public portal | `AKfycbxyuUXgK1oHUDMahQjluiL-gcrMK0qV0FWLFYaYBqGxlRSg9NhvmbyQRyf0dvaqg7Zp` | `@290`（変更なし）|
| Member split | `AKfycbxd_6HlH5aWLhxYOtLUHehI3ODiHg4fpc5SCzNdEBIDbDpaBuU3KTuqDRbeBmhWZxSQ_g` | `@40`（変更なし）|
| Admin split | `AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os` | `@51` |

## 4. Verification

- `npm run build:gas:admin`: PASS
- `npm run prerelease`: PASS
  - `security:audit`: high / critical gate PASS。`@google/clasp` 経由の `uuid` moderate finding は既知。
  - `security:public-boundary`: PASS
  - `security:split-boundary`: PASS
  - `typecheck`: PASS
- `npx clasp push --force`: admin split PASS
- `npx clasp version "v294 mailing list default selection and labels"`: admin split version `51` を作成
- `npx clasp redeploy ... --versionNumber 51`: admin fixed deployment を `@51` へ同期
- `npx clasp deployments --json`: admin fixed deployment が `@51` を指すことを確認済み

## 5. Browser Confirmation Pending

実ブラウザでの最終操作確認は操作者確認待ち。

- 管理者ポータルへログインできること。
- 宛名リスト出力コンソールで、発送区分選択後に候補一覧が表示され、初期選択件数が `0` であること。
- 「年度処理」の表示が残らず、「年会費納入」と表示されること。
- 「表示中を選択」ボタンが強調色なしで表示されること。
- 年会費納入フィルター、キーワード検索、対象選択、選択対象のみ Excel 出力が従来どおり動くこと。

## 6. Residual Tasks

- v291 から継続する必須 security backlog（Secret Manager 化・外部 KDF 検討）は `docs/172_DEFERRED_SECURITY_BACKLOG_SECRET_MANAGER_KDF_2026-05-01.md` で管理継続。
- Public OAuth scope minimization は継続課題。
- より広範な admin/member physical pruning は、過去の whiteout / dependency regression を踏まえて安全設計後に実施する。
