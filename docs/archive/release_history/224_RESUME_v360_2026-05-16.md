# v360 / v361 作業再開ガイド（履歴・現行入口ではない）

更新日: 2026-05-16（17:30 時点・v361 hotfix 反映済み）
状態: **履歴資料**（2026-05-18 時点の現行入口は `HANDOVER.md`）

> **2026-05-18 注記（重要）**: この文書は v361 時点の履歴資料。現行本番は v370 / integrated-public `@329` x2 / member `@87` / admin `@129`。次担当者の再開入口は `HANDOVER.md` と `docs/225_RELEASE_STATE_v360_to_v370_2026-05-17.md` を正とする。この文書内の v361 / `@319` / member `@76` / admin `@117` / 未コミット 22 files の記述は当時の状態であり、現行判断に使わない。

## ⏸ 中断時の状態（要点）

| 項目 | 状態 |
|---|---|
| 現行本番 | **v361** = v360 + import.meta hotfix |
| Deployments | integrated `@319` x2 / member `@76` / admin `@117` — 全 4 反映済み |
| Schema 自動初期化 | public / member は doGet 起動で完了。admin はブラウザ起動時に発火 |
| Schema データ migration | **未実行**（`runRebuildSchemaForV360`）→ 一括メール送信機能のみ動作不可 |
| 実ブラウザ動作確認 | **未実施** |
| 未コミット | 22 files（16 modified + 6 new）— working tree 残置 |

## 📜 直前に何が起きたか（短い物語）

1. v360 を全 4 deployments へ反映 → admin/member shell が **`SyntaxError: Cannot use 'import.meta' outside a module`** でクラッシュ
2. 即時ロールバック（4 deployments を v359 に redeploy）→ 1 分以内に復旧
3. 真因: **SheetJS xlsx の dynamic import** が Vite ビルドで `import.meta.url` を bundle に残し、`scripts/compress-html.mjs` が `new Function()` で eval する設計と非互換（v351 と同類）
4. xlsx を完全除去（CSV BOM で代替）+ `compress-html.mjs` に **build 時 `import.meta` 検出 gate** を追加
5. v361 として再 build → 全 3 bundle で **`import.meta: 0` を verified**
6. 4 deployments を v361 へ redeploy

詳細経緯と再発防止策: `memory/feedback_pdfjs_dist_vite_singlefile_trap.md`（v360 事例追記済み）

## 📦 未コミット変更（git status）

### Modified（16 件）
```
HANDOVER.md                              … v361 ステータス
backend/Code.gs                          … 公開 build artifact
backend/index_public.html                … 公開 HTML
docs/03_DATA_MODEL.md                    … §11 v360 Addendum
docs/learning/index.html                 … 14 / 15 のカード
gas-src/Code.full.gs                     … v360 主要変更（約 +1,100 行）
gas/admin/Code.gs                        … admin build artifact
gas/admin/index.html                     … admin HTML（xlsx 除去後）
gas/member/Code.gs                       … member build artifact
gas/member/index.html                    … member HTML（xlsx 除去後）
scripts/audit-admin-boundary.mjs         … v360 actions 11 件
scripts/build-admin-gas.mjs              … v360 actions 11 件
scripts/compress-html.mjs                … ★ v361 import.meta gate 追加
src/components/TrainingManagement.tsx    … 3 タブ化
src/services/api.ts                      … 11 wrapper
src/shared/types.ts                      … 8 型定義
```

> 注: `package.json` / `package-lock.json` は xlsx 除去後 v359 と同一に戻ったため未変更扱い。

### Untracked（6 件）
```
docs/223_RELEASE_STATE_v360_2026-05-16.md
docs/224_RESUME_v360_2026-05-16.md       … 本文書
docs/learning/14_data_model_v360_2026-05-16.html
docs/learning/15_system_overview_v360_2026-05-16.html
src/components/TrainingRoster.tsx
src/lib/xlsx.ts                          … 中身は CSV のみ
```

## ⏳ 残タスク（再開時の順序）

### 🅰️ Operator 担当（最優先 / 別案件後）

**① admin split で schema データ migration を 1 回 Run（必須）**

Apps Script editor で admin split プロジェクト（`AKfycbwS...` / scriptId `1tlBJ-OJjqN...`）を開き、関数 **`runRebuildSchemaForV360`** を 1 回だけ Run する。

実行内容:
- T_メール送信明細 をログ SS に作成（**これがないと一括メール送信が失敗する**）
- T_メール送信ログ.研修ID 列を追加
- 既存 EXTERNAL 申込行を 2-FK 化（申込者ID → 外部申込者ID 複写）
- 既存申込行の出欠状態を `UNRECORDED` で backfill
- ROSTER_TEMPLATE_LIST JSON に category 追加
- XOR 整合性監査

完了後 Logger.log で `xorViolations: 0` を確認。

**② admin shell ブラウザ起動確認**

```
https://script.google.com/a/macros/hcm-n.org/s/AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os/exec
```

→ コンソールエラーが出ず管理者ポータルが表示されること（v361 で復旧済みのはず）

**③ v360 機能の実ブラウザ動作確認**

- 研修管理 → 任意の研修 → 「名簿 / 出欠」タブが表示される
- 申込者一覧が表示される
- 行内の出欠ドロップダウンを変更 → 即時保存される
- CSV ダウンロードが正しい内容（Excel で開けることも確認）
- ゲスト追加 → T_外部申込者 + T_研修申込 に行が追加
- 一括メール送信（migration 後）→ T_メール送信明細 に記録される
- 既存の会員・public 側申込フローに影響なし
- スマホ実機（iPhone Safari / Android Chrome）でレスポンシブ崩れなし

**④ 全 PASS したら**

```bash
# 動作確認 PASS の報告とともに、明示的に「コミットして」と指示してください
# AGENTS.md に従い、明示指示なしでは commit しません
```

### 🅱️ 次回拡張（v362+ / 別チケット推奨）

GAS API は完成済。UI のみ追加すれば良い。

| 機能 | GAS API | UI |
|---|---|---|
| 会員選択モーダル（検索 UI） | `getAdminPersonList` 等流用可 | 現状 ID 直接入力 |
| TrainingMailSender セグメント絞り込み UI | `sendTrainingMailSegmented` 実装済 | 未実装 |
| メール送信ログ閲覧 UI | `getTrainingMailSendLogs` 実装済 | 未実装 |
| 会員詳細「受講履歴」タブ | `getMemberTrainingHistory` 実装済 | 未実装 |

### 🅲 後続クリーンアップ（v370+ 想定）

- `T_研修申込.申込者ID` / `申込者区分コード` の物理削除（v360 で deprecated マーキング済）
- 後方互換コード `getCanonicalApplicantRef_` の legacy fallback 除去

## 🔑 再開時に最初に読む順序

1. **本文書（`docs/224_RESUME_v360_2026-05-16.md`）** ← まずここ
2. `HANDOVER.md` の v361 行
3. `docs/223_RELEASE_STATE_v360_2026-05-16.md` — 変更詳細
4. `memory/feedback_pdfjs_dist_vite_singlefile_trap.md` — import.meta 罠
5. `docs/learning/14_data_model_v360_2026-05-16.html` — データモデル設計判断
6. `AGENTS.md` §0 シークレット最優先絶対ルール（毎回再確認）

## ⚠️ 再開時の注意

- **本番は既に v361 で稼働中**。コード変更しない限り再 deploy 不要
- 未コミットだが本番は live。**git checkout / reset で working tree を破壊しないこと**
- `runRebuildSchemaForV360` 実行前は一括メール送信機能のみ動作不可（他は OK）
- 別案件で `gas-src/Code.full.gs` / `src/components/TrainingManagement.tsx` / `scripts/compress-html.mjs` を触ると コンフリクト → 別ブランチ強く推奨
- `scripts/compress-html.mjs` の **import.meta gate を絶対に無効化しない**（v351 / v360 の再発防止策）

## 💾 別案件着手時の推奨

```bash
# Option A: 別ブランチで進める（強く推奨）
git checkout -b feature/<別案件名>
# 戻る時: git checkout main で v361 working tree 復活

# Option B: stash で待避
git stash push -u -m "v361-wip: training roster + import.meta hotfix"
# 戻る時: git stash pop

# Option C: 動作確認後にコミットしてから別案件へ（最も安全）
# user 明示指示 → 22 files commit → 別ブランチ
```

## 🔄 再開時クイックスタート

```bash
# 1. 本文書再読
cat docs/224_RESUME_v360_2026-05-16.md

# 2. 状態確認
git status --short
npm run typecheck && npm run security:public-boundary && npm run security:member-boundary && npm run security:admin-boundary

# 3. 全 PASS なら 🅰️ Operator タスクへ
#    （Apps Script editor で runRebuildSchemaForV360 Run → ブラウザ確認 → コミット）
```

## 📋 タスクリスト（再開時の参照用）

| # | 担当 | 内容 |
|---|---|---|
| 15 | operator | `runRebuildSchemaForV360` を admin split で 1 回 Run |
| 16 | operator | v361 実ブラウザ動作確認（admin shell 起動・名簿・出欠・CSV・メール・モバイル）|
| 17 | claude | 動作確認 OK 後にコミット（明示指示）|
| 18 | claude | v362+: MemberSelectModal / セグメントUI / 受講履歴タブ |
| 19 | claude | v370+: 旧 申込者ID / 申込者区分コード 物理削除 |

---

**2026-05-18 現在、この文書を最初の再開入口にしないこと。** 現行入口は `HANDOVER.md` と `docs/225_RELEASE_STATE_v360_to_v370_2026-05-17.md`。
