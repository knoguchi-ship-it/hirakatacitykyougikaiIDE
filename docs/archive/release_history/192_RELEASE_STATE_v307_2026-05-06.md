# Release State v307

作成日: 2026-05-06
対象版番: `v307`

## 1. Scope

管理コンソールの会員詳細編集画面に、会員ごとの年会費情報を表示・編集するセクションを追加した。

- 年度、会費、納入ステータス、納入確認日、備考を表示する。
- 納入ステータスは `未納` / `納入済み` を編集できる。
- `納入済み` の場合は納入確認日を必須にする。
- 備考は 2000 文字上限で編集できる。
- 年会費情報は会員基本情報とは独立して年度行ごとに保存する。

詳細: `docs/191_ADMIN_MEMBER_DETAIL_ANNUAL_FEE_EDIT_2026-05-06.md`

## 2. Versions

| Area | Version |
|---|---|
| Integrated/public GAS version | `290`（変更なし） |
| Member split GAS version | `44`（変更なし） |
| Admin split GAS version | `67` |

## 3. Fixed Deployments

| Area | Deployment ID | Version |
|---|---|---|
| Integrated legacy | `AKfycbywpWoYxij6A-ZunIeBjG1Q8qX78PMMTsT3frx1cM5PJ2nAuZpz81KruXb5LIvWgbQx` | `@290`（変更なし） |
| Public portal | `AKfycbxyuUXgK1oHUDMahQjluiL-gcrMK0qV0FWLFYaYBqGxlRSg9NhvmbyQRyf0dvaqg7Zp` | `@290`（変更なし） |
| Member split | `AKfycbxd_6HlH5aWLhxYOtLUHehI3ODiHg4fpc5SCzNdEBIDbDpaBuU3KTuqDRbeBmhWZxSQ_g` | `@44`（変更なし） |
| Admin split | `AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os` | `@67` |

## 4. Changed Files

| File | Change |
|---|---|
| `src/components/MemberDetailAdmin.tsx` | 年会費セクション、行単位編集・保存、入力検証、保存成功時の state 反映を追加。 |
| `gas/admin/index.html` | `npm run build:gas:admin` で admin UI artifact を再生成。 |
| `gas/admin/Code.gs` | `npm run build:gas:admin` で admin GAS artifact を再生成。 |
| `docs/191_ADMIN_MEMBER_DETAIL_ANNUAL_FEE_EDIT_2026-05-06.md` | 実装内容、設計メモ、確認観点を記録。 |
| `HANDOVER.md` / `docs/09_DEPLOYMENT_POLICY.md` / `docs/00_DOC_INDEX.md` | 現行本番を v307 / admin `@67` へ更新。 |

## 5. Verification

- `npm run security:audit`: PASS。high / critical はなし。moderate 3 件は残存（`ip-address` via `express-rate-limit` / `@modelcontextprotocol/sdk`）。
- `npm run security:public-boundary`: PASS
- `npm run typecheck`: PASS
- `npm run build:gas:admin`: PASS
- `npm run security:split-boundary`: PASS
- `cd gas/admin && npx clasp push --force`: PASS
- `cd gas/admin && npx clasp version "v307 admin member detail annual fee edit"`: PASS。Created version `67`
- `cd gas/admin && npx clasp redeploy AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os --versionNumber 67 --description "v307 admin member detail annual fee edit"`: PASS
- `cd gas/admin && npx clasp deployments --json`: admin fixed deployment `@67` を確認。
- `cd gas/member && npx clasp deployments --json`: member fixed deployment `@44` 維持を確認。
- `npx clasp deployments --json`: integrated/public fixed deployments `@290` x2 維持を確認。

補足:

- `npx clasp version` / `npx clasp redeploy` は通常経路で Apps Script API の空エラーが出たため、承認済みの安定実行経路で再実行し PASS。

## 6. Operator Verification Pending

実ブラウザ確認は操作者側で実施する。

- 管理コンソールで会員詳細編集を開き、年会費セクションが表示されること。
- `未納` / `納入済み`、納入確認日、備考を変更して年度行ごとに保存できること。
- `納入済み` で納入確認日が空の場合、保存されずエラーが表示されること。
- 保存後に年会費管理コンソール側へ同じ内容が反映されること。
- 保存後に `unsupported_action` の全画面エラーにならないこと。
