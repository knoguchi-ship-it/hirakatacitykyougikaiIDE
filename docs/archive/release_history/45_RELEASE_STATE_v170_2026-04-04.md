# Release State v170

作成日: 2026-04-04
対象: 本番 `v170` / fixed deployments `@170`
ステータス: 保存版

## 1. この文書の目的
- 2026-04-04 時点の本番コード状態と DB 状態を同時に固定記録する。
- 次担当者が「コードは v170、DB は同日ロールバック後の整合済み状態」という前提で再開できるようにする。

## 2. 本番コード状態
- 管理コンソールの年度別会員表示は、current status ではなく当該年度時点の在籍状態で判定する。
- ダッシュボードの `ALL` 選択時ヘッダーは `全期間` と表示する。
- fixed deployment 2 本はともに `@170`。

## 3. 本番 DB 状態
- 2026-04-04 中に実施された DB 復旧作業は、最終的にユーザー判断でロールバックされた。
- 現在は、ユーザーが整合性確認済みの DB 状態を本番基準とする。
- 今後の DB 変更は、この状態を起点として差分管理する。

## 4. 当日確認済み事項
- `npx clasp deployments --json`: member / public ともに `@170`
- `npx clasp run healthCheck`: 成功
- `npx clasp run getDbInfo`: 成功
- `npm run typecheck`: 成功
- `npm run build`: 成功
- `npm run build:gas`: 成功

## 5. 次担当者への注意
- DB 変更前には、必ず事前バックアップ取得とロールバック手順を記録する。
- `seedDemoData()` は本番固定 DB を破壊するため、通常運用では実行しない。
- 現在の正本は `HANDOVER.md`、`docs/09_DEPLOYMENT_POLICY.md`、`docs/04_DB_OPERATION_RUNBOOK.md`、`docs/20_NEXT_INSTRUCTIONS_FOR_CLAUDECODE_2026-03-19.md` を優先参照する。
