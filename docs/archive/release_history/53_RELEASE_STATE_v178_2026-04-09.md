# Release State v178

Date: 2026-04-09
Version: `178`
Fixed deployments: member `@178` / public `@178`

## Summary
- Fixed business staff count on admin dashboard: previous implementation iterated over portal-scoped `members` state (only the logged-in user's organization), so it only counted that org's 4 staff. Changed to aggregate `enrolledStaffCount` from `filteredAdminMemberRows`, which is sourced from backend-computed per-org counts.
- Backend now includes `enrolledStaffCount` (ENROLLED staff in each BUSINESS org) in each `AdminDashboardMemberRow`.
- Changed admin console default filter from "全期間・全種別・在籍中" to "最新年度・全種別・在籍中". `hasFilteredView` judgment updated to use the new defaults as baseline.

## Changed Files
- `backend/Code.gs`: Added `enrolledStaffCountByMember` map and `enrolledStaffCount` field in `memberSummaries`.
- `src/types.ts`: Added `enrolledStaffCount?: number` to `AdminDashboardMemberRow`.
- `src/App.tsx`: Simplified `filteredDashboardBusinessStaffCount` to sum `enrolledStaffCount` from `filteredAdminMemberRows`. Changed `DEFAULT_MEMBER_FISCAL_YEAR_FILTER` to current fiscal year. Added `DEFAULT_MEMBER_STATUS_FILTER = 'ACTIVE'`. Updated `hasFilteredView` comparison.

## Verification
- `npm run typecheck` — PASS
- `npm run build` — PASS
- `npm run build:gas` — PASS
- `npx clasp push --force` — 4 files pushed
- `npx clasp version "v178 fix business staff count and set default filter to current fiscal year"` — version 178 created
- `npx clasp redeploy <member> --versionNumber 178` — PASS
- `npx clasp redeploy <public> --versionNumber 178` — PASS
- `npx clasp deployments --json` — both @178 confirmed
- `npx clasp run healthCheck` — ok
- `npx clasp run getDbInfo` — 29 sheets confirmed

## Deployment IDs
- Member portal: `AKfycbywpWoYxij6A-ZunIeBjG1Q8qX78PMMTsT3frx1cM5PJ2nAuZpz81KruXb5LIvWgbQx`
- Public portal: `AKfycbxyuUXgK1oHUDMahQjluiL-gcrMK0qV0FWLFYaYBqGxlRSg9NhvmbyQRyf0dvaqg7Zp`

## Pending (browser confirmation)
- Admin login → admin console → confirm "事業所職員" count > "事業所会員" count (e.g., 30 orgs should show significantly more staff).
- Confirm default filter opens as "2026年度・全種別・在籍中" without requiring manual selection.
- Note: GAS cache TTL is 300 seconds; if old data shows immediately after release, wait 5 minutes and reload.
