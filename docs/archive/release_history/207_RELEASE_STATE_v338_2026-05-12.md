# Release State v338 - Admin Workplace Search Fix

Date: 2026-05-12
Status: deployed

## Scope

- Fixed admin member list keyword search for individual / support members by returning workplace office name in `AdminDashboardMemberRow.officeName`.
- Fixed annual fee management keyword search by returning workplace office name in `AnnualFeeAdminRecord.officeName`.
- Changed both backend mappings from `T_会員.事業所名` to `T_会員.勤務先名`.
- Bumped the admin dashboard cache key suffix to avoid stale cached rows with empty `officeName`.
- No public portal or member portal functional change.

## Deployment

| Target | Version |
|---|---|
| integrated-public fixed deployments | `@299` x2 (unchanged) |
| member split fixed deployment | `@55` (unchanged) |
| admin split fixed deployment | `@96` |

Admin fixed deployment:

- `AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os @96`
- Description: `v338 workplace search fix`

## Verification

- `npm run prerelease`: PASS
- `npm run build:gas`: PASS
- `npm run build:gas:admin`: PASS
- `npm run security:admin-boundary`: PASS
- `npx clasp push --force` in `gas/admin`: PASS
- `npx clasp version "v338 fix admin workplace search payload"`: created version `96`
- `npx clasp redeploy ... --versionNumber 96 --description "v338 workplace search fix"`: PASS
- `npx clasp deployments --json`: admin fixed deployment confirmed at `versionNumber: 96`

## Operator Checks

Pending browser-side checks:

1. Admin portal -> 会員管理 -> 会員一覧
2. Search an individual / support member by workplace office name
3. Confirm the member appears
4. Repeat in 年会費管理
5. Confirm existing member number / name searches still work
