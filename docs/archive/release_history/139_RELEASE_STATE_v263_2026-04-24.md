# Release State: v263

リリース日: 2026-04-24
本番バージョン: `v263`
GAS バージョン: 統合プロジェクト @265 / 会員 split @19 / 管理者 split @23

---

## 変更内容

### 1. OAuth スコープ最小化（docs/123 完了）

各プロジェクトの `appsscript.json` から不要スコープを削除。

| プロジェクト | 削除したスコープ |
|---|---|
| 統合（公開） | `script.projects` `script.deployments` `script.webapp.deploy` `gmail.send` `gmail.settings.basic` `logging.read` `userinfo.email` `userinfo.profile` `cloud-platform` |
| 管理者 split | `gmail.send` |
| 会員 split | `drive` `userinfo.email` `userinfo.profile` |

最小構成:
- 統合: `spreadsheets` `script.external_request` `script.send_mail` `drive`
- 管理者: `spreadsheets` `script.external_request` `script.send_mail` `gmail.settings.basic` `drive` `userinfo.email`
- 会員: `spreadsheets` `script.external_request` `script.send_mail`

### 2. CI セキュリティゲート追加（docs/123 完了）

`package.json` に追加:
- `security:audit`: `npm audit --audit-level=high`（High 以上の脆弱性を検出）
- `security:secrets`: シークレットファイルの誤コミット検出
- `prerelease`: `security:audit` + `typecheck` の連結ゲート

`docs/09_DEPLOYMENT_POLICY.md` に追記:
- リリース前チェックに `npm run security:audit` を必須化
- シークレットファイル誤コミット確認コマンドを追加

### 3. CM番号編集ポリシー確定（docs/113 案C実装確認・ポリシー文書化）

**案C（管理者のみ変更可）** を正式採用。

実装状態確認:
- `MEMBER_WRITABLE_FIELDS_` に `careManagerNumber` なし → 会員セルフサービスから変更不可（既実装）
- `submittedStaff` mapping に `careManagerNumber` なし → 職員間変更も不可（既実装）
- `MemberForm.tsx`: 入力欄 `disabled={true}` + 説明文表示（既実装）

今回の追加:
- `MEMBER_WRITABLE_FIELDS_` の直前にポリシーコメントを追加
- `docs/05_AUTH_AND_ROLE_SPEC.md` §9 に CM番号編集ポリシーを正式記録

---

## デプロイ記録

```bash
# 統合プロジェクト
npx clasp push --force
npx clasp version "v263 OAuthスコープ最小化・CM番号編集ポリシー確定・セキュリティゲートCI追加"
# → version 265
npx clasp redeploy AKfycbywpWoYxij6A-ZunIeBjG1Q8qX78PMMTsT3frx1cM5PJ2nAuZpz81KruXb5LIvWgbQx --versionNumber 265
npx clasp redeploy AKfycbxyuUXgK1oHUDMahQjluiL-gcrMK0qV0FWLFYaYBqGxlRSg9NhvmbyQRyf0dvaqg7Zp --versionNumber 265

# 会員 split
cd gas/member && npx clasp push --force
npx clasp version "v263 OAuthスコープ最小化・CM番号編集ポリシー確定"
# → version 19
npx clasp redeploy AKfycbxd_6HlH5aWLhxYOtLUHehI3ODiHg4fpc5SCzNdEBIDbDpaBuU3KTuqDRbeBmhWZxSQ_g --versionNumber 19

# 管理者 split
cd gas/admin && npx clasp push --force
npx clasp version "v263 OAuthスコープ最小化・CM番号編集ポリシー確定"
# → version 23
npx clasp redeploy AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os --versionNumber 23
```

---

## 動作確認事項

- スコープ削減により既存の OAuth grant が失効する可能性がある。
  失効した場合は `myaccount.google.com/permissions` でアプリのアクセス権を手動で失効させ、再承認する。
- 管理者ポータルで `gmail.settings.basic`（送信エイリアス取得）が引き続き動作すること
- 会員マイページで CM番号が読み取り専用で表示されること
- `npm run security:audit` が clean で通過すること

---

## 完了したセキュリティタスク一覧（第三者評価 docs/109 起票分）

| タスク | 内容 | 完了バージョン |
|---|---|---|
| docs/120 | processApiRequest deny-by-default | v261（実装確認・archive） |
| docs/121 | 自己操作 API IDOR 修正 | v261（実装確認・archive） |
| docs/122 | パスワードハッシュ PBKDF2 移行 | v262 |
| docs/123 | OAuth スコープ最小化・CI | v263（本リリース） |
| docs/113 | CM番号編集ポリシー実装・文書化 | v263（本リリース） |
