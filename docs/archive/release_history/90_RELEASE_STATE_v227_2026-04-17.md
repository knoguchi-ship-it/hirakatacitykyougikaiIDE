# Release State v227

Updated: 2026-04-17
Version: `227`
Fixed deployments: member `@227` / public `@227`

## Summary

v227 makes the public portal copy configurable from the admin settings screen and cleans up the default wording for the membership card.

## Included Changes

### 1. Public portal hero copy settings

- Added `T_システム設定.PUBLIC_PORTAL_*` keys for the public portal hero section.
- The top helper badge can now be shown or hidden.
- The top title can be edited from the admin settings screen.
- The top description can now be shown or hidden independently.

### 2. Membership card copy settings

- Added settings for the membership card badge, title, description, and CTA label.
- Each text block can be edited without code changes.
- Badge/title/description visibility can be toggled individually.

### 3. Default copy cleanup

- `APPLICATION PORTAL` is now hidden by default.
- `MEMBERSHIP` is replaced by the Japanese default `入会申込`.
- The redundant line `ログイン不要で申込できます` is removed.
- The membership CTA is now `入会申込へ進む`.

### 4. Admin settings UI

- The system settings screen now includes a dedicated public portal copy section.
- Operators can manage hero copy and membership card copy in one place.
- Empty inputs safely fall back to default values.

## Verification

- `npm run typecheck` ✅
- `npm run build` ✅
- `npm run build:gas` ✅
- `npx clasp push --force` ✅
- `npx clasp version "v227 public portal copy settings and japanese membership label"` ✅
- `npx clasp redeploy` member ✅ `@227`
- `npx clasp redeploy` public ✅ `@227`
- `npx clasp deployments --json` ✅ both `@227`
- `npx clasp run healthCheck` ❌ blocked by known project-scoped OAuth / permission issue
- `npx clasp run getDbInfo` ❌ blocked by the same issue

## Impact Notes

- Frontend + settings-layer change. No member/auth flow change.
- No DB schema migration required. New `T_システム設定` keys are appended lazily by `ensureSystemSettingsRows_`.
- The public portal now supports copy changes without further code deploys for the configured fields.
- Apps Script runtime verification remains partially blocked until the operator restores project-scoped OAuth access.
