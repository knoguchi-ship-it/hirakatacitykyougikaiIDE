# Release State: v250 (2026-04-20)

## 概要

管理者 split プロジェクトで `member_unauthorized` エラーが表示されていた根本原因を修正。

## 変更内容

### 症状
- 管理者 split プロジェクト（`gas/admin`）にアクセスし、Google アカウントでログイン成功後、
  メインコンテンツに `member_unauthorized` エラーが赤いボックスで表示されていた。
- サイドバーに「会員マイページ」「研修受講の申込み」が表示されていた（管理者専用画面に不要）。

### 根本原因（2件）

1. **`src/components/Sidebar.tsx`**: `menuItems` 配列に `profile` / `training-apply` が無条件で含まれていた。
   `showMemberPages` prop は定義されていたが、`menuItems` 構築に反映されていなかった。

2. **`src/App.tsx` useEffect (line ~668)**: `currentView === 'profile'` のとき `loadMemberPortalData` を呼ぶ
   ガードに `isAdminShell` チェックがなかった。管理者ログイン直後に React の初期 state
   `currentView='profile'` が一瞬残り、sessionToken なしで会員 API が呼ばれ `member_unauthorized` が返っていた。

### 修正

| ファイル | 変更箇所 | 内容 |
|---|---|---|
| `src/components/Sidebar.tsx` | `menuItems` 構築 | `profile` / `training-apply` を `showMemberPages` フラグで条件化 |
| `src/App.tsx` | `<Sidebar>` 呼び出し | `showMemberPages={!isAdminShell}` を追加 |
| `src/App.tsx` | useEffect (会員データ取得) | `!isAdminShell &&` ガードを追加 |

## デプロイ状態

- **対象**: 管理者 split プロジェクト（`gas/admin`）
- **push**: `gas/admin/` 3 files (appsscript.json / Code.gs / index.html)
- **version**: 3（`v250 fix admin shell: hide member menu, guard getMemberPortalData`）
- **fixed deployment**: `AKfycbwSCTTyvWY_cFG764XawdbqA8r0qxYbav4aDZ-BK9rRmvXHoUXrKQnQ9egRGqWcx4Os` → `@3`

### 影響しない deployment（変更なし）
- **会員マイページ固定 URL** (`AKfycbxy...`): v249 @249 のまま
- **公開ポータル固定 URL** (`AKfycbyw...`): v249 @249 のまま
- ※ 元の統合プロジェクトの fixed deployment は変更なし

## 実ブラウザ確認（操作者）

- [ ] 管理者 split URL（`AKfycbwS.../exec`）にアクセス
- [ ] `k.noguchi@hcm-n.org` で Google ログイン
- [ ] サイドバーに「会員マイページ」「研修受講の申込み」が **表示されない** こと
- [ ] メインコンテンツに `member_unauthorized` エラーが **表示されない** こと
- [ ] 「管理コンソール（会員管理）」などの管理者メニューが正常表示されること
- [ ] 会員マイページ固定 URL は変更なく動作すること

## セキュリティ設計確定事項（このセッションで確定）

### admin / member / public 3境界分離（v250〜）

| 境界 | プロジェクト | アクセス制御 | 認証方式 | 用途 |
|---|---|---|---|---|
| admin | gas/admin（split） | DOMAIN | Google セッション | 管理機能専用 |
| member | gas/member（split）/ 統合 | ANYONE_ANONYMOUS | ID/PW | 会員機能専用 |
| public | 統合 | ANYONE_ANONYMOUS | なし | 入会申込のみ |

**確定根拠**: `docs/109_THIRD_PARTY_ASSESSMENT_2026-04-20.md`（第三者評価）・`docs/111_IMPLEMENTATION_BLUEPRINT_PROJECT_SPLIT_2026-04-20.md`（実装設計）。

**グランドルール（`AGENTS.md` セクション6 追加済み）**: 確定済みセキュリティ境界に反する案を「選択肢の一つ」として対等に提示しない。利便性はセキュリティ境界を崩す理由にならない。

### 管理者と会員マイページの完全分離

管理者は管理 URL（admin split）を、会員としての操作は会員 URL を別途使用する。同一画面での混在は禁止。

## 残課題（次フェーズ）

- CM番号編集ポリシー（`docs/113_TASK_CM_NUMBER_EDIT_POLICY_2026-04-20.md`）
- パスワードハッシュ化 Phase 2（PBKDF2-SHA256）
- OAuth scope 最小化 Phase 4
- WCAG 2.2 AA 対応 Phase 4
- 公開ポータルの物理プロジェクト移行（現在は暫定延期）
