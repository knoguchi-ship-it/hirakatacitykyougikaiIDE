# 引き継ぎ書（2026-03-01）

## 1. 現在地（要約）
- 対象: 枚方市介護支援専門員連絡協議会 会員システム（GAS + Spreadsheet DB）
- 作業ディレクトリ: `C:\VSCode\CloudePL\hirakatacitykyougikaiIDE`
- 標準GCPプロジェクト: `uguisu-gas-exec-20260225191000`
- Apps Script ID: `11YRlyWVgWRFw5_zByfLnA_vUlZzLeBSgiaanQCvZZoHMAfay8yK7RdkL`

## 2. 今回（2026-03-01）反映した内容
1. ドキュメント更新（完了）
- `docs/05_AUTH_AND_ROLE_SPEC.md` に設定済みサービス/権限一覧、確認手順を追記
- `docs/08_GCP_SETUP_RUNBOOK_2026-02-28.md` を新規作成（画像付き）
- GCP証跡画像を `docs/assets/gcp-setup-2026-02-28/` に格納

2. 実装更新（完了）
- `backend/Code.gs` に以下を実装
  - `seedDemoData()`（本番DBにモック相当データ投入）
  - `fetchAllDataFromDb_()`（スプレッドシートDBから画面用データを生成）
  - `getApiDataSnapshot()`（検証用）
  - `verifySeedData()`（件数/メール/ログインID確認用）
- `processApiRequest('fetchAllData')` がDB実データを返すよう変更

3. データ投入と検証（完了）
- Apps Script エディタで `seedDemoData` 実行: 完了
- Apps Script エディタで `verifySeedData` 実行: 完了
- 実行ログ確認結果:
  - `memberCount: 3`
  - `staffCount: 3`
  - `authCount: 6`
  - `trainingCount: 2`
  - `annualFeeCount: 6`
  - `loginIds`: `member-12345678`, `member-87654321`, `office-99999999-admin`, `office-99999999-s2`, `office-99999999-s3`
  - `distinctEmails`: `k.noguchi@uguisunosato.or.jp` のみ
  - `allEmailsAreNoguchi: true`

4. リポジトリ反映（完了）
- GitHub `main` へ push 済み
- 主要コミット:
  - `2f64b5f` docs更新
  - `2b14301` GAS実装（シード + DB返却）

## 3. 現在の設定状態（確認済み）
1. GCP API
- `script.googleapis.com`: 有効
- `drive.googleapis.com`: 有効
- `logging.googleapis.com`: 有効

2. Google Auth Platform
- 対象: `内部`
- OAuthクライアント: `admin-google-login-web` が存在

3. Apps Script
- 高度なサービスに `Drive` が追加済み
- `backend/Code.gs` は DBシード/DB返却ロジックを含む最新版（commit `2b14301`）

## 4. 運用ルール（固定）
- 会員ログインは `ログインID + パスワード` のみ（Googleログイン不使用）
- 管理者ログインのみ Google 認証 + ホワイトリスト突合
- 管理者ログイン時も会員ページを表示（会員ID紐付け必須）
- 会費は引き落としなし。未納時のみ振込口座表示

## 5. 次スレッド開始時に貼る最小情報
1. 「参照ドキュメント」
- `docs/05_AUTH_AND_ROLE_SPEC.md`
- `docs/08_GCP_SETUP_RUNBOOK_2026-02-28.md`
- `docs/99_HANDOVER_2026-03-01.md`
2. 「作業対象ブランチ名」
3. 「今回の目的（1-3行）」

## 6. スレッドクリア推奨判断
- 推奨: `クリア推奨`
- 理由:
  - 既に認証・デプロイ・設計議論が長く積み上がっており、文脈が広すぎるため
  - 次フェーズ（実装/検証）で判断の混線リスクを下げるため
- クリア前条件:
  - 本ファイルと `docs/05`, `docs/08` が最新であること（今回更新済み）

## 7. 既知の注意点
- `gcloud services list` はローカル権限で失敗する場合あり（credentials.db権限エラー）
- `npx clasp apis` は Apps Script 側の有効化確認には有効
- 既存ワークツリーには本件と無関係な変更が混在しているため、コミット時は対象ファイルを限定すること
- `npx clasp run` はスコープ同意不足時に `AuthRequiredError` になるため、Apps Script UI実行で回避している

## 8. 未解消事項（最優先）
1. 本番Webアプリ `/exec` が 404
- 確認日時: 2026-03-01
- 事象:
  - デプロイ更新後も `https://script.google.com/macros/s/{deploymentId}/exec` が
    `https://script.google.com/a/macros/uguisunosato.or.jp/s/.../exec` へリダイレクト後、404
- 補足:
  - 同じデプロイの Execution API URL はデプロイ画面に表示される
  - DBシードおよび検証関数は Apps Script UI上で正常実行済み

## 9. 次担当の再開手順（この順で実施）
1. Apps Script > `Deploy` > `Manage deployments` で `Webアプリ` 種別の現行デプロイを確認
2. `New deployment` で Webアプリを新規発行し、発行直後URLを検証
3. `/exec` が200表示できるIDを特定したら、そのIDに `@12` 以降の最新版を再デプロイ
4. 表示検証:
- 個人会員 / 事業所会員（管理者） / 事業所会員（メンバー）
- ログインID表示
- 未納会員で振込口座表示
- 研修詳細・PDF表示
