# Release State: v193

作成日: 2026-04-09
リリースバージョン: v193
固定 deployment: member `@193` / public `@193`

## 実施内容

### v192: 管理者ログイン分離

`handleAdminSessionLogin` (App.tsx) で呼んでいた `adminLoginWithData`（`checkAdminBySession_` + `getMemberPortalData_` の一括実行）を `checkAdminBySession` のみに変更。

- 管理者ログインはセッション認証のみ（auth result を返す）
- 会員リストは遷移後の遅延取得に分離
- Playwright 確認: 管理者コンソール（176会員・SVGチャート）が正常表示

### v193: 管理者認証キャッシュ

`checkAdminBySession_()` (Code.gs) に `CacheService.getScriptCache()` を用いたキャッシュを追加。

#### キャッシュ対象

| テーブル | キャッシュキー | TTL |
|---|---|---|
| `T_管理者Googleホワイトリスト` | `admin_wl_v1` | 300秒 |
| `T_認証アカウント` | `admin_auth_v1` | 300秒 |
| 会員名（表示用） | `fetchAllDataFromDb_()` キャッシュから取得 | 既存キャッシュ依存 |

#### 無効化タイミング

- `saveAdminPermission_()`: ホワイトリスト保存後に `admin_wl_v1` を削除
- `deleteAdminPermission_()`: ホワイトリスト削除後に `admin_wl_v1` を削除
- 認証アカウントはTTL自然失効（5分）で対応（変更が稀なため）

#### 期待効果

| ケース | 最適化前 | 最適化後 |
|---|---|---|
| コールドスタート（GAS V8 再起動） | ~25s | ~5-8s（スクリプト初期化分は不変） |
| ウォームインスタンス（キャッシュヒット） | ~3-5s | ~1-2s |
| ウォームインスタンス（キャッシュ失効） | ~3-5s | ~3-5s（変化なし） |

GAS スクリプトのコールドスタート自体（V8 runtime + 9000行スクリプト読み込み）は20s前後かかる可能性があり、これは改善不能。ウォームインスタンスではキャッシュが有効に機能する。

## 確認済み

- `npm run typecheck`: PASS
- `npm run build:gas`: PASS
- `npx clasp push`: Pushed 4 files
- `npx clasp version 193`: Created
- `npx clasp redeploy @193`: 両 deployment 反映済み
- `npx clasp run healthCheck`: OK (2026-04-09T14:33:34.008Z)

## 要ブラウザ実確認

- [ ] 管理者ログインが成功する（Googleアカウントでログイン）
- [ ] 管理者コンソールが正常表示される（会員リスト・SVGチャート）
- [ ] 2回目の管理者ログインが1回目より速い（キャッシュヒット確認）
- [ ] 会員ログイン（`demo-ind-001` / `demo1234`）が正常動作する

## 残課題

- **B-02**: `fetchAllDataFromDbFresh_()` 全シート一括取得最適化（Phase 3）
- **GEMINI_API_KEY**: Script Properties 設定（AI メール機能を使う場合）
